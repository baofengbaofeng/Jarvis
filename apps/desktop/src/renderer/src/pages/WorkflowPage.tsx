import { WorkflowEditor } from '../components/workflow/WorkflowEditor';

// F10 (M8 Task 9): /workflow route — DAG workflow visual editor + runner.
export function WorkflowPage() {
  return <WorkflowEditor />;
}
