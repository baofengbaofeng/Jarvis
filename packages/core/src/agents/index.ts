// L30 (M8 Task 8): agent template library. Pure data + seed (no node:*), so it
// is exported from BOTH core barrels (index.ts for the main process, renderer.ts
// for renderer-safe imports).
export * from './templates';
