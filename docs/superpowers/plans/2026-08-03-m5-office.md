# M5 办公增强 (Office) 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 本计划依赖 M0–M4:`packages/core`(ModelRouter/ChatService/ContextManager/structuredChat)、main IPC、SQLite schema v1(chat_messages/agents/tasks/prompt_templates 表)、渲染层 store 模式、i18n 框架、M1 web_search 工具。
>
> **覆盖能力(§10.6 M5 行)**:D4–D12、D15、I8、L21–L23、L25。D3 联网搜索(M1 已实现)本里程碑仅补 L25 搜索源配置;D13 Markdown(D1)/D14 导出(M1)不再重复。Q5:B — 全局系统 Hook(A3/I1–I4)推迟 V2.0,D4 为 App 内划词。

**Goal:** 补齐办公场景闭环:划词菜单(翻译/解释/总结/搜索)、AI 写作与边写边译、PDF 伴读、内置 WebView 浏览器与网页一键总结、视频链接总结、文生图、语音输入、文件上传分析(PDF/Word/Excel/PPT)、多模态识图、拖拽处理、Prompt 模板库、全局 FTS 搜索与联网搜索源配置。

**Architecture:** 办公逻辑以 `packages/core/src/office/` 纯函数/类承载(可单测):prompt 构建、段落切分、PDF 分页、网页正文提取、视频链接解析、图片生成 adapter(fetch 注入)、语音封装、文件分类/解析路由、多模态 content[]、拖拽决策、模板变量、FTS 辅助、搜索源请求构建。Electron main 提供 IPC(office.*)与 WebViewHost(session 隔离的简易浏览器 I8);渲染层为各 Feature 组件。解析器(mammoth/xlsx/pdfjs/jszip)仅 main 侧引入,core 以注入接口隔离。

**Tech Stack:** M0–M4 技术栈 + `pdfjs-dist`(PDF 渲染+取文)、`mammoth`(docx)、`xlsx`(SheetJS,excel)、`jszip`+极简 XML 提取(pptx)、Web Speech API(`webkitSpeechRecognition`)、Electron `BrowserWindow`+`session.fromPartition`(WebView)、FTS5(SQLite 内置)。

## Global Constraints

(继承 M0–M4 全部约束。M5 相关复述:)

- **D4 划词:** App 内划词/粘贴分析;系统级全局 Hook 依赖 I1,推迟 V2.0。
- **I8 内置浏览器:** 简易 WebView,用于 D8 网页总结;session 隔离;不做通用 Browser Agent 自动化。
- **D9 视频总结:** App 内粘贴链接/上传;transcript 依赖可选 API/Whisper,未配置则返回清晰错误(不静默失败)。
- **D10 文生图:** Provider image API 抽象;Anthropic 无图像生成时仅 OpenAI 兼容端点(可扩展)。
- **D11 语音输入:** Web Speech API 优先,Whisper local 可选;L24 TTS 明确排除。
- **L23 多模态:** 消息 content 扩展为 `content[]`(`text`/`image_url`);OpenAI/Anthropic adapter 均须映射。
- **L21 FTS5:** chat_messages/agents/tasks 建 FTS5 虚表 + 触发器保持同步;每表单写者(§13.3)不变。
- **L25 搜索源:** `search_providers` 配置(settings JSON);web_search 按配置路由;未配置回退 M1 默认。
- **L22 拖拽:** 图片/办公文档 → 附件分析;其它文件 → 拷贝进工作区。
- **D15 模板库:** `prompt_templates` 表(main 属主)+ `{{var}}` 变量替换。
- **i18n:** M5 新增 UI(划词菜单/写作/PDF/浏览器/文生图/语音/搜索源/模板库)须 zh-CN/en 对称。

## 文件结构总览(本里程碑新增)

```
packages/core/src/office/
├── officeChat.ts / spec          # chatText 流式收集辅助
├── selection.ts / spec           # D4 划词 prompt 构建
├── writing.ts / spec             # D5/D6 写作/边写边译
├── pdf.ts / spec                 # D7 分页/摘要 prompt
├── webpage.ts / spec             # D8 extractMainText 纯函数
├── video.ts / spec               # D9 链接解析/metadata
├── image.ts / spec               # D10 OpenAI image adapter
├── speech.ts / spec              # D11 Web Speech 封装
├── files.ts / spec               # D12 文件分类/解析路由/pptx
├── content.ts / spec             # L23 content[] + normalize
├── dropzone.ts / spec            # L22 拖拽决策
├── templates.ts / spec           # D15 模板变量
├── search.ts / spec              # L21 FTS 辅助
├── searchProvider.ts / spec      # L25 搜索源请求构建/解析
└── index.ts
apps/desktop/src/main/
├── ipc/office.ts                 # office.* 分析通道
├── ipc/templates.ts              # D15 CRUD + render
├── ipc/search.ts                 # L21 globalSearch
├── webview/WebViewHost.ts        # I8 会话隔离浏览器
├── db/migrations.ts              # 追加 v3:FTS5 虚表 + 触发器
└── IpcRouter.ts
apps/desktop/src/renderer/src/
├── components/office/
│   ├── SelectionMenu.tsx / spec  # D4
│   ├── WritingView.tsx / spec    # D5/D6
│   ├── VoiceInputButton.tsx / spec  # D11
│   ├── DropZone.tsx / spec       # L22
│   ├── ImageAttachment.tsx / spec   # L23
│   └── SearchProvidersPage.tsx   # L25
├── pages/OfficePage.tsx          # 办公聚合入口
├── pages/PromptTemplatesPage.tsx # D15
├── pages/PdfReaderPage.tsx       # D7
└── OfficePage.spec.tsx
```

---

### Task 1: 划词(D4)— SelectionMenu + 分析通道

**Files:**
- Create: `packages/core/src/office/officeChat.ts`
- Create: `packages/core/src/office/officeChat.spec.ts`
- Create: `packages/core/src/office/selection.ts`
- Create: `packages/core/src/office/selection.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/office/SelectionMenu.tsx`
- Create: `apps/desktop/src/renderer/src/components/office/SelectionMenu.spec.tsx`

**Interfaces:**
- Consumes: M1 ModelRouter(chat 流式)。
- Produces:
  - `chatText(router, messages): Promise<string>` — 收集 `deltaText` 拼全文。
  - `SelectionAction = 'translate'|'explain'|'summarize'|'search'`;`buildSelectionPrompt(req): string`。
  - main IPC `office.selection { text, action, targetLang? }` → `chatText` 返回结果。
  - `SelectionMenu`(mouseup 划词浮层,4 操作 → invoke `office.selection` → onResult)。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/selection.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildSelectionPrompt } from './selection';
import { chatText } from './officeChat';

describe('selection prompts', () => {
  it('builds per-action prompts', () => {
    expect(buildSelectionPrompt({ text: 'hello world', action: 'translate', targetLang: '中文' })).toContain('中文');
    expect(buildSelectionPrompt({ text: 'x', action: 'summarize' })).toContain('要点');
    expect(buildSelectionPrompt({ text: 'x', action: 'search' })).toContain('搜索词');
  });
});

describe('chatText', () => {
  it('collects streaming deltas', async () => {
    async function* fake(): AsyncIterable<{ deltaText?: string }> { yield { deltaText: 'a' }; yield { deltaText: 'b' }; }
    expect(await chatText({ chat: async () => fake() } as never, [{ role: 'user', content: 'hi' }])).toBe('ab');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/officeChat.ts`:
```ts
export interface ChatRouterLike { chat(req: { messages: Array<{ role: string; content: unknown }> }): AsyncIterable<{ deltaText?: string }> }

export async function chatText(router: ChatRouterLike, messages: Array<{ role: string; content: unknown }>): Promise<string> {
  let out = '';
  for await (const c of router.chat({ messages })) out += c.deltaText ?? '';
  return out.trim();
}
```

`packages/core/src/office/selection.ts`:
```ts
export type SelectionAction = 'translate' | 'explain' | 'summarize' | 'search';
export interface SelectionRequest { text: string; action: SelectionAction; targetLang?: string }

export function buildSelectionPrompt(req: SelectionRequest): string {
  switch (req.action) {
    case 'translate': return `请把下面文本翻译成${req.targetLang ?? '中文'},只输出译文:\n\n${req.text}`;
    case 'explain': return `请解释下面文本的含义、上下文与要点:\n\n${req.text}`;
    case 'summarize': return `请用 3-5 条要点总结下面文本:\n\n${req.text}`;
    case 'search': return `请基于下面文本生成 2-3 个可直接搜索的关键词(每行一个,不要解释):\n\n${req.text}`;
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office`
Expected: PASS。

- [ ] **Step 5: main IPC + 组件**

`apps/desktop/src/main/ipc/office.ts`:
```ts
import { chatText, buildSelectionPrompt, type SelectionAction } from '@jarvis/core';

export function registerOfficeIpc(router: { register(ch: string, h: (...a: unknown[]) => unknown): void }, modelRouter: { chat(req: unknown): AsyncIterable<{ deltaText?: string }> }) {
  router.register('office.selection', async (_e, req: { text: string; action: SelectionAction; targetLang?: string }) => {
    const prompt = buildSelectionPrompt(req);
    const result = await chatText(modelRouter, [{ role: 'system', content: '你是专业助手。' }, { role: 'user', content: prompt }]);
    return { ok: true, result };
  });
}
```

`apps/desktop/src/renderer/src/components/office/SelectionMenu.tsx`:
```tsx
import { useEffect, useRef, useState } from 'react';
import { useTranslation } from 'react-i18next';
import type { SelectionAction } from '@jarvis/core';

const ACTIONS: Array<{ key: SelectionAction; label: string }> = [
  { key: 'translate', label: '译' }, { key: 'explain', label: '释' },
  { key: 'summarize', label: '总' }, { key: 'search', label: '搜' }
];

export function SelectionMenu() {
  const { t } = useTranslation('common');
  const [pos, setPos] = useState<{ x: number; y: number; text: string } | null>(null);
  const [result, setResult] = useState<string>('');
  const root = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const onMouseUp = () => {
      const sel = window.getSelection();
      const text = sel?.toString().trim() ?? '';
      if (text.length > 0) setPos({ x: sel!.getRangeAt(0).getBoundingClientRect().right + 8, y: sel!.getRangeAt(0).getBoundingClientRect().top });
      else setPos(null);
    };
    document.addEventListener('mouseup', onMouseUp);
    return () => document.removeEventListener('mouseup', onMouseUp);
  }, []);

  const run = async (action: SelectionAction) => {
    if (!pos) return;
    const r = (await window.jarvis.invoke('office.selection', { text: pos.text, action })) as { ok: boolean; result?: string };
    if (r.ok) setResult(r.result ?? '');
    setPos(null);
  };

  return (
    <div ref={root} data-testid="selection-menu">
      {pos && (
        <div className="selection-menu" style={{ left: pos.x, top: pos.y }}>
          {ACTIONS.map(a => <button key={a.key} data-testid={`sel-${a.key}`} onClick={() => void run(a.key)}>{t(`selection.${a.key}`, a.label)}</button>)}
        </div>
      )}
      {result && <div data-testid="selection-result" className="selection-result">{result}</div>}
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/office/SelectionMenu.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { SelectionMenu } from './SelectionMenu';

describe('SelectionMenu', () => {
  it('invokes office.selection on action click', async () => {
    const invoke = vi.fn(async () => ({ ok: true, result: '译文' }));
    (window as unknown as { jarvis: unknown }).jarvis = { invoke, onDidReceive: () => () => {} };
    render(<SelectionMenu />);
    // 直接触发组件内部逻辑:先模拟一次 selection
    window.getSelection = () => ({ toString: () => 'hello', getRangeAt: () => ({ getBoundingClientRect: () => ({ left: 0, top: 0, right: 10 }) }) } as unknown as Selection);
    document.dispatchEvent(new MouseEvent('mouseup'));
    const btn = screen.getByTestId('sel-translate');
    fireEvent.click(btn);
    expect(invoke).toHaveBeenCalledWith('office.selection', expect.objectContaining({ action: 'translate' }));
  });
});
```

- [ ] **Step 6: 运行测试确认通过 + i18n 对称**

zh-CN:`"selection.translate": "翻译"`,等;en 对应。
Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/office/SelectionMenu.spec.tsx && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/office apps/desktop/src/main/ipc/office.ts apps/desktop/src/renderer/src/components/office/SelectionMenu.tsx apps/desktop/src/renderer/src/components/office/SelectionMenu.spec.tsx apps/desktop/src/main/ipc/IpcRouter.ts packages/i18n/locales
git commit -m "feat(office): selection menu with translate/explain/summarize/search (D4)"
```

---

### Task 2: AI 写作(D5)+ 边写边译(D6)

**Files:**
- Create: `packages/core/src/office/writing.ts`
- Create: `packages/core/src/office/writing.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/office/WritingView.tsx`
- Create: `apps/desktop/src/renderer/src/components/office/WritingView.spec.tsx`

**Interfaces:**
- Consumes: Task 1 `chatText`。
- Produces:
  - `WritingAction = 'polish'|'continue'|'summarize'|'translate'`;`buildWritingPrompt(action, text, lang?): string`
  - `splitParagraphs(text): string[]`
  - `translateWhileTyping(text, targetLang, translate): Promise<{ done: string[]; pending: string }>` — 完整段落翻译,末段视为进行中。
  - main IPC `office.writing { action, text, lang? }` → chatText。
  - `WritingView`:textarea + 工具条(润色/续写/总结/翻译)+ 边写边译开关(debounce 800ms 调 translateWhileTyping,实时渲染 done + pending)。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/writing.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildWritingPrompt, splitParagraphs, translateWhileTyping } from './writing';

describe('writing', () => {
  it('builds prompts per action', () => {
    expect(buildWritingPrompt('polish', 'hi', )).toContain('润色');
    expect(buildWritingPrompt('translate', 'hi', 'en')).toContain('en');
  });

  it('splits paragraphs on blank lines', () => {
    expect(splitParagraphs('a\n\nb\n\nc')).toEqual(['a', 'b', 'c']);
  });

  it('translates complete paragraphs, leaves last pending', async () => {
    const tr = async (p: string) => `T:${p}`;
    const r = await translateWhileTyping('a\n\nb\n\nc', 'en', tr);
    expect(r.done).toEqual(['T:a', 'T:b']);
    expect(r.pending).toBe('c');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/writing.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/writing.ts`:
```ts
export type WritingAction = 'polish' | 'continue' | 'summarize' | 'translate';

export function buildWritingPrompt(action: WritingAction, text: string, lang?: string): string {
  switch (action) {
    case 'polish': return `请润色下面的文字,使其更流畅专业,保留原意,只输出润色结果:\n\n${text}`;
    case 'continue': return `请接着下面的文字自然续写一段,保持风格一致:\n\n${text}`;
    case 'summarize': return `请总结下面文字的要点:\n\n${text}`;
    case 'translate': return `请将下面的文字翻译成${lang ?? '中文'}:\n\n${text}`;
  }
}

export function splitParagraphs(text: string): string[] {
  return text.split(/\n{2,}/).map(s => s.trim()).filter(Boolean);
}

export interface TranslateResult { done: string[]; pending: string }

export async function translateWhileTyping(text: string, targetLang: string, translate: (p: string) => Promise<string>): Promise<TranslateResult> {
  const paras = splitParagraphs(text);
  if (paras.length <= 1) return { done: [], pending: text };
  const done: string[] = [];
  for (const p of paras.slice(0, -1)) done.push(await translate(p));
  return { done, pending: paras[paras.length - 1] };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/writing.spec.ts`
Expected: PASS。

- [ ] **Step 5: main IPC + WritingView**

`apps/desktop/src/main/ipc/office.ts` 追加:
```ts
router.register('office.writing', async (_e, req: { action: 'polish' | 'continue' | 'summarize' | 'translate'; text: string; lang?: string }) => {
  const result = await chatText(modelRouter, [{ role: 'user', content: buildWritingPrompt(req.action, req.text, req.lang) }]);
  return { ok: true, result };
});
router.register('office.writing.translate', async (_e, text: string, lang: string) => {
  const { done, pending } = await translateWhileTyping(text, lang, async (p) => {
    return chatText(modelRouter, [{ role: 'user', content: buildWritingPrompt('translate', p, lang) }]);
  });
  return { ok: true, done, pending };
});
```

`apps/desktop/src/renderer/src/components/office/WritingView.tsx`:
```tsx
import { useEffect, useRef, useState } from 'react';
import { useTranslation } from 'react-i18next';
import type { WritingAction } from '@jarvis/core';

export function WritingView() {
  const { t } = useTranslation('common');
  const [text, setText] = useState('');
  const [live, setLive] = useState<{ done: string[]; pending: string } | null>(null);
  const [sideBySide, setSideBySide] = useState(false);
  const debounceRef = useRef<ReturnType<typeof setTimeout>>();

  const runAction = async (action: WritingAction) => {
    if (!text.trim()) return;
    const r = (await window.jarvis.invoke('office.writing', { action, text })) as { ok: boolean; result?: string };
    if (r.ok && r.result) setText(r.result);
  };

  useEffect(() => {
    if (!sideBySide || !text.trim()) { setLive(null); return; }
    if (debounceRef.current) clearTimeout(debounceRef.current);
    debounceRef.current = setTimeout(async () => {
      const r = (await window.jarvis.invoke('office.writing.translate', text, 'en')) as { ok: boolean; done: string[]; pending: string };
      if (r.ok) setLive({ done: r.done, pending: r.pending });
    }, 800);
    return () => { if (debounceRef.current) clearTimeout(debounceRef.current); };
  }, [text, sideBySide]);

  return (
    <div data-testid="writing-view">
      <textarea data-testid="writing-text" value={text} onChange={e => setText(e.target.value)} rows={12} />
      <div>
        <button data-testid="writing-polish" onClick={() => void runAction('polish')}>{t('writing.polish')}</button>
        <button data-testid="writing-continue" onClick={() => void runAction('continue')}>{t('writing.continue')}</button>
        <button data-testid="writing-summarize" onClick={() => void runAction('summarize')}>{t('writing.summarize')}</button>
        <button data-testid="writing-translate" onClick={() => void runAction('translate')}>{t('writing.translate')}</button>
        <label><input type="checkbox" data-testid="writing-live" checked={sideBySide} onChange={e => setSideBySide(e.target.checked)} /> {t('writing.live')}</label>
      </div>
      {live && <div data-testid="writing-live-result">{live.done.join('\n')}<span className="pending">{live.pending}</span></div>}
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/office/WritingView.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { WritingView } from './WritingView';

describe('WritingView', () => {
  it('invokes office.writing on action', async () => {
    const invoke = vi.fn(async () => ({ ok: true, result: '润色后' }));
    (window as unknown as { jarvis: unknown }).jarvis = { invoke, onDidReceive: () => () => {} };
    render(<WritingView />);
    fireEvent.change(screen.getByTestId('writing-text'), { target: { value: 'some text' } });
    fireEvent.click(screen.getByTestId('writing-polish'));
    expect(invoke).toHaveBeenCalledWith('office.writing', expect.objectContaining({ action: 'polish' }));
  });
});
```

- [ ] **Step 6: 运行测试确认通过 + i18n 对称**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/office/WritingView.spec.tsx && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/office/writing.ts packages/core/src/office/writing.spec.ts apps/desktop/src/main/ipc/office.ts apps/desktop/src/renderer/src/components/office/WritingView.tsx apps/desktop/src/renderer/src/components/office/WritingView.spec.tsx packages/i18n/locales
git commit -m "feat(office): AI writing actions and side-by-side translate while typing (D5/D6)"
```

---

### Task 3: PDF 伴读(D7)— 分页取文 + 摘要

**Files:**
- Create: `packages/core/src/office/pdf.ts`
- Create: `packages/core/src/office/pdf.spec.ts`
- Create: `apps/desktop/src/renderer/src/pages/PdfReaderPage.tsx`

**Interfaces:**
- Consumes: Task 1 `chatText`。
- Produces:
  - `buildPdfSummaryPrompt(title, pageRange, pageTexts): string` — 中英对照结构化要点。
  - `chunkPages(pageTexts, maxChars?): Array<{ from; to; texts }>` — 按字符预算分页组。
  - main IPC `office.pdf.extract { path }` → pdfjs-dist 取每页文本;`office.pdf.summarize { path, from, to }` → chunk + chatText。
  - `PdfReaderPage`:pdfjs-dist 渲染页面 canvas + "本页/全篇摘要"按钮 + 结果面板。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/pdf.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { chunkPages, buildPdfSummaryPrompt } from './pdf';

describe('pdf helpers', () => {
  it('chunks pages by char budget', () => {
    const pages = ['a'.repeat(3000), 'b'.repeat(3000), 'c'.repeat(3000)];
    const chunks = chunkPages(pages, 4000);
    expect(chunks.length).toBeGreaterThan(1);
    expect(chunks[0].from).toBe(1);
    expect(chunks[0].to).toBe(1);
  });

  it('builds a structured summary prompt', () => {
    const p = buildPdfSummaryPrompt('book', { from: 1, to: 2 }, ['page one', 'page two']);
    expect(p).toContain('book');
    expect(p).toContain('page one');
    expect(p).toContain('中英对照');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/pdf.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/pdf.ts`:
```ts
export interface PdfMeta { pages: number; title?: string }

export function chunkPages(pageTexts: string[], maxChars = 4000): Array<{ from: number; to: number; texts: string[] }> {
  const out: Array<{ from: number; to: number; texts: string[] }> = [];
  let cur: { from: number; to: number; texts: string[] } | null = null;
  let acc = 0;
  pageTexts.forEach((t, i) => {
    if (!cur) cur = { from: i + 1, to: i + 1, texts: [] };
    if (acc + t.length > maxChars && cur.texts.length) {
      out.push(cur);
      cur = { from: i + 1, to: i + 1, texts: [] };
      acc = 0;
    }
    acc += t.length;
    cur.to = i + 1;
    cur.texts.push(t);
  });
  if (cur) out.push(cur);
  return out;
}

export function buildPdfSummaryPrompt(title: string | undefined, pageRange: { from: number; to: number }, pageTexts: string[]): string {
  const body = pageTexts.map((t, i) => `【第 ${pageRange.from + i} 页】\n${t}`).join('\n');
  return `请总结以下 PDF${title ? `《${title}》` : ''}第 ${pageRange.from}-${pageRange.to} 页的内容,输出结构化的中英对照要点:\n\n${body}`;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/pdf.spec.ts`
Expected: PASS。

- [ ] **Step 5: main PDF IPC + PdfReaderPage**

`apps/desktop/src/main/ipc/office.ts` 追加:
```ts
import { getDocument } from 'pdfjs-dist';
router.register('office.pdf.extract', async (_e, path: string) => {
  const data = new Uint8Array(readFileSync(path));
  const doc = await getDocument({ data }).promise;
  const pageTexts: string[] = [];
  for (let i = 1; i <= doc.numPages; i++) {
    const page = await doc.getPage(i);
    const content = await page.getTextContent();
    pageTexts.push(content.items.map((it: { str?: string }) => it.str ?? '').join(' '));
  }
  return { ok: true, pages: doc.numPages, pageTexts };
});
router.register('office.pdf.summarize', async (_e, path: string, from: number, to: number) => {
  const ext = await (await import('./office')) as never; // 复用 extract 逻辑,或独立实现
  const { pageTexts } = (await extractPdf(path)) as { pageTexts: string[] };
  const chunk = chunkPages(pageTexts.slice(from - 1, to));
  const out: string[] = [];
  for (const c of chunk) out.push(await chatText(modelRouter, [{ role: 'user', content: buildPdfSummaryPrompt(undefined, { from: c.from + from - 1, to: c.to + from - 1 }, c.texts) }]));
  return { ok: true, result: out.join('\n\n---\n\n') };
});
```
(把 `extractPdf(path)` 抽为独立辅助函数,office.extract 与其复用。)

`apps/desktop/src/renderer/src/pages/PdfReaderPage.tsx`(要点):
```tsx
import { useEffect, useRef, useState } from 'react';
import * as pdfjs from 'pdfjs-dist';
pdfjs.GlobalWorkerOptions.workerSrc = new URL('pdfjs-dist/build/pdf.worker.min.mjs', import.meta.url).toString();

export function PdfReaderPage() {
  const [path, setPath] = useState<string>('');
  const [summary, setSummary] = useState('');
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const renderPage = async (p: number) => {
    const ext = (await window.jarvis.invoke('office.pdf.extract', path)) as { ok: boolean; pageTexts?: string[] };
    // 渲染第 p 页到 canvas:重新 getDocument → getPage(p) → render({ canvasContext, viewport })
  };
  const summarize = async () => {
    const r = (await window.jarvis.invoke('office.pdf.summarize', path, 1, 9999)) as { ok: boolean; result?: string };
    if (r.ok) setSummary(r.result ?? '');
  };
  return (
    <div data-testid="pdf-reader">
      <input data-testid="pdf-path" value={path} onChange={e => setPath(e.target.value)} />
      <canvas ref={canvasRef} data-testid="pdf-canvas" />
      <button data-testid="pdf-summarize" onClick={() => void summarize()}>摘要</button>
      <div data-testid="pdf-summary">{summary}</div>
    </div>
  );
}
```

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/office/pdf.ts packages/core/src/office/pdf.spec.ts apps/desktop/src/main/ipc/office.ts apps/desktop/src/renderer/src/pages/PdfReaderPage.tsx
git commit -m "feat(office): pdf page extraction, chunking and summarization (D7)"
```

---

### Task 4: WebView 浏览器(I8)+ 网页一键总结(D8)

**Files:**
- Create: `packages/core/src/office/webpage.ts`
- Create: `packages/core/src/office/webpage.spec.ts`
- Create: `apps/desktop/src/main/webview/WebViewHost.ts`

**Interfaces:**
- Consumes: Task 1 `chatText`。
- Produces:
  - `extractMainText(html: string): string` — 纯函数正文提取(去 script/style/标签,取长文本块),可单测。
  - `WebViewHost { open(url, onExtract?): Promise<void>; extract(): Promise<string>; close(): void }` — `BrowserWindow` + `session.fromPartition` 隔离;`executeJavaScript` 提取正文。
  - main IPC `office.webview.open { url }`、`office.webview.summarize { url }`(open → extract → chatText → close)。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/webpage.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { extractMainText } from './webpage';

const html = `
<html><head><style>body{}</style><script>var x=1;</script></head>
<body>
  <nav>menu menu menu menu</nav>
  <article>
    <h1>标题</h1>
    <p>这是正文第一段,包含足够多的文字内容以便被选中为正文主体。</p>
    <p>这是正文第二段,继续提供更多有意义的句子来支撑正文提取逻辑的判断。</p>
  </article>
  <footer>footer links</footer>
</body></html>`;

describe('extractMainText', () => {
  it('drops scripts/styles/nav and keeps the article body', () => {
    const text = extractMainText(html);
    expect(text).toContain('这是正文第一段');
    expect(text).toContain('这是正文第二段');
    expect(text).not.toContain('var x=1');
    expect(text).not.toContain('menu menu');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/webpage.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/webpage.ts`:
```ts
export function extractMainText(html: string): string {
  const noScript = html.replace(/<script[\s\S]*?<\/script>/gi, ' ').replace(/<style[\s\S]*?<\/style>/gi, ' ');
  const noTags = noScript.replace(/<[^>]+>/g, ' ');
  const cleaned = noTags.replace(/&nbsp;/g, ' ').replace(/&#?\w+;/g, ' ').replace(/[ \t]+/g, ' ');
  const blocks = cleaned.split(/\s*\n\s*/).map(s => s.trim()).filter(s => s.length > 40);
  if (blocks.length === 0) return cleaned.replace(/\s+/g, ' ').trim();
  return blocks.sort((a, b) => b.length - a.length).slice(0, 5).join('\n');
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/webpage.spec.ts`
Expected: PASS。

- [ ] **Step 5: WebViewHost + IPC**

`apps/desktop/src/main/webview/WebViewHost.ts`:
```ts
import { BrowserWindow, session } from 'electron';

export class WebViewHost {
  private win: BrowserWindow | null = null;

  async open(url: string): Promise<void> {
    const ses = session.fromPartition(`webview-${Date.now()}`, { cache: false }); // I8 会话隔离
    this.win = new BrowserWindow({ width: 900, height: 700, webPreferences: { session: ses, sandbox: true } });
    this.win.on('closed', () => { this.win = null; });
    await this.win.loadURL(url);
  }

  async extract(): Promise<string> {
    if (!this.win) return '';
    const js = `(() => { const el = document.querySelector('article, main, [role=main]') || document.body; return el.innerText.slice(0, 20000); })()`;
    return (await this.win.webContents.executeJavaScript(js)) as string;
  }

  close(): void { this.win?.close(); this.win = null; }
  isOpen(): boolean { return this.win !== null; }
}
```

`apps/desktop/src/main/ipc/office.ts` 追加:
```ts
router.register('office.webview.open', async (_e, url: string) => { await webViewHost.open(url); return { ok: true }; });
router.register('office.webview.summarize', async (_e, url: string) => {
  await webViewHost.open(url);
  const raw = await webViewHost.extract();
  const text = extractMainText(raw) || raw; // 生产优先 extract() 的 innerText;extractMainText 为纯函数兜底
  webViewHost.close();
  const result = await chatText(modelRouter, [{ role: 'user', content: `请总结下面网页内容,给出要点:\n\n${text.slice(0, 12000)}` }]);
  return { ok: true, result };
});
```
(注:WebViewHost 为 Electron 集成,人工验证;单测覆盖 `extractMainText`。)

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/office/webpage.ts packages/core/src/office/webpage.spec.ts apps/desktop/src/main/webview/WebViewHost.ts apps/desktop/src/main/ipc/office.ts
git commit -m "feat(office): session-isolated WebView and one-click page summary (I8/D8)"
```

---

### Task 5: 视频总结(D9)+ 文生图(D10)

**Files:**
- Create: `packages/core/src/office/video.ts`
- Create: `packages/core/src/office/video.spec.ts`
- Create: `packages/core/src/office/image.ts`
- Create: `packages/core/src/office/image.spec.ts`

**Interfaces:**
- Consumes: Task 1 `chatText`。
- Produces:
  - `parseVideoUrl(url): { platform: 'youtube'|'bilibili'|'unknown'; id: string|null }`
  - `fetchVideoMeta(parse, oembed): Promise<{ platform; id; title? }>` — oEmbed 取标题。
  - `summarizeVideo(meta, transcript?): string | Error` — 有 transcript 生成摘要 prompt;无则抛清晰错误。
  - `ImageAdapter { generate(req): Promise<Array<{ url }>> }`;`createOpenAiImageAdapter(deps)` — fetch 注入,dall-e-3。
  - main IPC `office.video.summarize { url }`、`office.image.generate { prompt, size? }`。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/video.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseVideoUrl, fetchVideoMeta, summarizeVideo } from './video';

describe('video', () => {
  it('parses youtube and bilibili urls', () => {
    expect(parseVideoUrl('https://youtu.be/abc123').id).toBe('abc123');
    expect(parseVideoUrl('https://www.youtube.com/watch?v=xyz456').id).toBe('xyz456');
    expect(parseVideoUrl('https://www.bilibili.com/video/BV1xx411c7mD').id).toBe('BV1xx411c7mD');
    expect(parseVideoUrl('https://example.com/x').platform).toBe('unknown');
  });

  it('fetches metadata title via oembed', async () => {
    const meta = await fetchVideoMeta(parseVideoUrl, async () => ({ title: 'How to X' }));
    expect(meta.title).toBe('How to X');
  });

  it('errors clearly when no transcript is available', () => {
    const meta = { platform: 'youtube' as const, id: 'abc123' };
    expect(() => summarizeVideo(meta, undefined)).toThrow('transcript');
    const p = summarizeVideo(meta, 'full transcript text');
    expect(p).toContain('full transcript text');
  });
});
```

`packages/core/src/office/image.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { createOpenAiImageAdapter } from './image';

describe('image adapter', () => {
  it('posts to images/generations and returns urls', async () => {
    const fetchImpl = async (url: string, init?: RequestInit) => ({
      ok: true, status: 200, json: async () => ({ data: [{ url: 'https://img/x.png' }] }), text: async () => ''
    }) as Response;
    const adapter = createOpenAiImageAdapter({ apiKey: 'sk-test', fetchImpl: fetchImpl as never });
    const urls = await adapter.generate({ prompt: 'a cat' });
    expect(urls[0].url).toContain('img/x.png');
  });

  it('throws on non-ok response', async () => {
    const fetchImpl = async () => ({ ok: false, status: 500, json: async () => ({}), text: async () => 'boom' }) as Response;
    const adapter = createOpenAiImageAdapter({ apiKey: 'k', fetchImpl: fetchImpl as never });
    await expect(adapter.generate({ prompt: 'x' })).rejects.toThrow('500');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/video.spec.ts src/office/image.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/video.ts`:
```ts
export type VideoPlatform = 'youtube' | 'bilibili' | 'unknown';
export interface VideoMeta { platform: VideoPlatform; id: string | null; title?: string }

export function parseVideoUrl(url: string): VideoMeta {
  const yt = /(?:youtube\.com\/watch\?v=|youtu\.be\/)([\w-]{6,})/.exec(url);
  if (yt) return { platform: 'youtube', id: yt[1] };
  const bl = /bilibili\.com\/video\/(BV[\w]+)/.exec(url);
  if (bl) return { platform: 'bilibili', id: bl[1] };
  return { platform: 'unknown', id: null };
}

export async function fetchVideoMeta(
  parse: (url: string) => VideoMeta,
  oembed: (url: string) => Promise<{ title?: string } | null>,
): Promise<VideoMeta> {
  const meta = parse(url);
  if (meta.platform === 'unknown') return meta;
  const res = await oembed(url);
  return { ...meta, title: res?.title };
}

export function summarizeVideo(meta: VideoMeta, transcript: string | undefined): string {
  if (!transcript || transcript.trim().length < 20) {
    throw new Error('未配置 transcript API/Whisper,无法获取视频字幕(见设置→办公→视频)。');
  }
  return `请总结以下视频${meta.title ? `《${meta.title}》` : ''}(${meta.platform} ${meta.id})的要点:\n\n${transcript.slice(0, 12000)}`;
}
```

`packages/core/src/office/image.ts`:
```ts
export interface ImageRequest { prompt: string; size?: '256x256' | '512x512' | '1024x1024'; n?: number }
export interface ImageAdapter { generate(req: ImageRequest): Promise<Array<{ url: string }>> }
export class ImageError extends Error {}

export interface OpenAiImageDeps { apiKey: string; baseUrl?: string; model?: string; fetchImpl?: typeof fetch }

export function createOpenAiImageAdapter(deps: OpenAiImageDeps): ImageAdapter {
  const f = deps.fetchImpl ?? fetch;
  return {
    async generate(req) {
      const res = await f(`${deps.baseUrl ?? 'https://api.openai.com/v1'}/images/generations`, {
        method: 'POST',
        headers: { 'content-type': 'application/json', authorization: `Bearer ${deps.apiKey}` },
        body: JSON.stringify({ model: deps.model ?? 'dall-e-3', prompt: req.prompt, size: req.size ?? '1024x1024', n: req.n ?? 1 })
      });
      if (!res.ok) throw new ImageError(`image gen failed ${res.status}: ${await res.text()}`);
      const data = (await res.json()) as { data: Array<{ url: string }> };
      return data.data;
    }
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/video.spec.ts src/office/image.spec.ts`
Expected: PASS。

- [ ] **Step 5: main IPC**

`apps/desktop/src/main/ipc/office.ts` 追加:
```ts
router.register('office.video.summarize', async (_e, url: string) => {
  const meta = await fetchVideoMeta(parseVideoUrl, async (u) => {
    const r = await fetch(`https://www.youtube.com/oembed?url=${encodeURIComponent(u)}&format=json`).catch(() => null);
    return r && r.ok ? (await r.json() as { title?: string }) : null;
  });
  const transcript = getTranscript(meta); // 可选:transcript API/Whisper;无则 undefined
  const prompt = summarizeVideo(meta, transcript);
  const result = await chatText(modelRouter, [{ role: 'user', content: prompt }]);
  return { ok: true, meta, result };
});
router.register('office.image.generate', async (_e, req: { prompt: string; size?: string }) => {
  const key = resolveApiKey('image'); // M1 Keychain
  const urls = await createOpenAiImageAdapter({ apiKey: key }).generate({ prompt: req.prompt, size: req.size as never });
  return { ok: true, urls };
});
```

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/office/video.ts packages/core/src/office/video.spec.ts packages/core/src/office/image.ts packages/core/src/office/image.spec.ts apps/desktop/src/main/ipc/office.ts
git commit -m "feat(office): video link summary and image generation adapter (D9/D10)"
```

---

### Task 6: 语音输入(D11)

**Files:**
- Create: `packages/core/src/office/speech.ts`
- Create: `packages/core/src/office/speech.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/office/VoiceInputButton.tsx`
- Create: `apps/desktop/src/renderer/src/components/office/VoiceInputButton.spec.tsx`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `isWebSpeechAvailable(): boolean`
  - `SpeechRecognitionLike`;`createSpeechRecognition(getCtor, lang?): SpeechRecognitionLike`
  - `attachTranscriptListener(rec, onFinal): void` — 把识别结果(final)回调给上层。
  - `VoiceInputButton({ onText })`:按下开始识别、松手/完成把最终文本交给聊天输入框。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/speech.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { isWebSpeechAvailable, attachTranscriptListener, type SpeechRecognitionLike } from './speech';

describe('speech', () => {
  it('detects absence of Web Speech in node', () => {
    expect(isWebSpeechAvailable()).toBe(false);
  });

  it('forwards final transcript via listener', () => {
    let onresult: ((ev: { results: ArrayLike<{ 0: { transcript: string }; isFinal: boolean }> }) => void) | null = null;
    const rec: SpeechRecognitionLike = {
      lang: 'zh-CN', interimResults: true, continuous: false,
      onresult: null, onend: null, onerror: null,
      start() {}, stop() {}
    };
    attachTranscriptListener(rec, (t) => captured.push(t));
    const captured: string[] = [];
    // attach 后再触发
    if (rec.onresult) rec.onresult({ results: [{ 0: { transcript: '你好' }, isFinal: true }] });
    expect(captured).toEqual(['你好']);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/speech.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/speech.ts`:
```ts
export interface SpeechRecognitionLike {
  lang: string; interimResults: boolean; continuous: boolean;
  onresult: ((ev: { results: ArrayLike<{ 0: { transcript: string }; isFinal: boolean }> }) => void) | null;
  onend: (() => void) | null;
  onerror: ((ev: { error: string }) => void) | null;
  start(): void; stop(): void;
}

export function isWebSpeechAvailable(): boolean {
  if (typeof window === 'undefined') return false;
  const w = window as unknown as { SpeechRecognition?: unknown; webkitSpeechRecognition?: unknown };
  return !!(w.SpeechRecognition || w.webkitSpeechRecognition);
}

export function createSpeechRecognition(getCtor: () => new () => SpeechRecognitionLike, lang = 'zh-CN'): SpeechRecognitionLike {
  const rec = new (getCtor())();
  rec.lang = lang;
  rec.interimResults = true;
  rec.continuous = false;
  return rec;
}

export function attachTranscriptListener(rec: SpeechRecognitionLike, onFinal: (text: string) => void): void {
  rec.onresult = (ev) => {
    let text = '';
    for (let i = 0; i < ev.results.length; i++) text += ev.results[i][0].transcript;
    const last = ev.results[ev.results.length - 1];
    if (last && last.isFinal) onFinal(text.trim());
  };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/speech.spec.ts`
Expected: PASS。

- [ ] **Step 5: 组件 + 冒烟测试**

`apps/desktop/src/renderer/src/components/office/VoiceInputButton.tsx`:
```tsx
import { useRef, useState } from 'react';
import { isWebSpeechAvailable, createSpeechRecognition, attachTranscriptListener, type SpeechRecognitionLike } from '@jarvis/core';

export function VoiceInputButton({ onText }: { onText: (text: string) => void }) {
  const [listening, setListening] = useState(false);
  const recRef = useRef<SpeechRecognitionLike | null>(null);

  const start = () => {
    if (!isWebSpeechAvailable()) return;
    const ctor = () => {
      const w = window as unknown as { SpeechRecognition?: new () => SpeechRecognitionLike; webkitSpeechRecognition?: new () => SpeechRecognitionLike };
      return (w.SpeechRecognition ?? w.webkitSpeechRecognition)!;
    };
    const rec = createSpeechRecognition(ctor);
    attachTranscriptListener(rec, (t) => { if (t) onText(t); });
    rec.onend = () => setListening(false);
    recRef.current = rec;
    rec.start();
    setListening(true);
  };
  const stop = () => { recRef.current?.stop(); setListening(false); };

  return (
    <button data-testid="voice-input" onMouseDown={start} onMouseUp={stop} className={listening ? 'is-listening' : ''}>
      {listening ? '●' : '🎙'}
    </button>
  );
}
```

`apps/desktop/src/renderer/src/components/office/VoiceInputButton.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { VoiceInputButton } from './VoiceInputButton';

describe('VoiceInputButton', () => {
  it('no-ops when Web Speech is unavailable', () => {
    const onText = vi.fn();
    render(<VoiceInputButton onText={onText} />);
    fireEvent.mouseDown(screen.getByTestId('voice-input'));
    expect(onText).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/office/VoiceInputButton.spec.tsx`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/office/speech.ts packages/core/src/office/speech.spec.ts apps/desktop/src/renderer/src/components/office/VoiceInputButton.tsx apps/desktop/src/renderer/src/components/office/VoiceInputButton.spec.tsx
git commit -m "feat(office): web speech voice input (D11)"
```

---

### Task 7: 文件上传分析(D12)+ 拖拽(L22)

**Files:**
- Create: `packages/core/src/office/files.ts`
- Create: `packages/core/src/office/files.spec.ts`
- Create: `packages/core/src/office/dropzone.ts`
- Create: `packages/core/src/office/dropzone.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/office/DropZone.tsx`
- Create: `apps/desktop/src/renderer/src/components/office/DropZone.spec.tsx`

**Interfaces:**
- Consumes: Task 1 `chatText`;M4 workspace IPC。
- Produces:
  - `OfficeFileKind`;`classifyFile(name): OfficeFileKind`
  - `extractFileText(file, extractors, deps?): Promise<string>` — 按 kind 路由解析器。
  - `extractPptxText(xml): string`;`extractPptx(buffer, unzip): Promise<string>` — jszip 解包 slide XML 取文本。
  - `decideDrop(files, opts): { attach; copyToWorkspace }` — 图片/办公文档 → attach;其它 → 拷贝进工作区。
  - main IPC `office.file.analyze { path }`(pdfjs/mammoth/xlsx/jszip 解析 + chatText)、`workspace.copyFiles { paths }`。
  - `DropZone`:包裹聊天输入,drop → decideDrop → attach 图片/文档或拷贝工作区。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/files.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { classifyFile, extractFileText, extractPptxText } from './files';

describe('office files', () => {
  it('classifies by extension', () => {
    expect(classifyFile('a.pdf')).toBe('pdf');
    expect(classifyFile('b.docx')).toBe('docx');
    expect(classifyFile('c.xlsx')).toBe('xlsx');
    expect(classifyFile('d.pptx')).toBe('pptx');
    expect(classifyFile('e.png')).toBe('image');
    expect(classifyFile('f.txt')).toBe('other');
  });

  it('routes to the matching extractor', async () => {
    const extractors = { docx: async () => 'doc text' };
    expect(await extractFileText({ path: '/x/a.docx', name: 'a.docx' }, extractors)).toBe('doc text');
    await expect(extractFileText({ path: '/x/a.pdf', name: 'a.pdf' }, extractors)).rejects.toThrow('unsupported');
  });

  it('extracts text from pptx slide xml', () => {
    expect(extractPptxText('<a:t>Hello</a:t><a:t>World</a:t>')).toBe('Hello\nWorld');
  });
});
```

`packages/core/src/office/dropzone.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { decideDrop } from './dropzone';

describe('dropzone', () => {
  it('attaches images/docs and copies other files to workspace', () => {
    const d = decideDrop([{ name: 'a.png' }, { name: 'b.docx' }, { name: 'notes.txt' }]);
    expect(d.attach.map(f => f.name)).toEqual(['a.png', 'b.docx']);
    expect(d.copyToWorkspace.map(f => f.name)).toEqual(['notes.txt']);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/files.spec.ts src/office/dropzone.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/files.ts`:
```ts
export type OfficeFileKind = 'pdf' | 'docx' | 'xlsx' | 'pptx' | 'image' | 'other';

export function classifyFile(name: string): OfficeFileKind {
  const ext = name.split('.').pop()?.toLowerCase() ?? '';
  if (ext === 'pdf') return 'pdf';
  if (ext === 'docx' || ext === 'doc') return 'docx';
  if (ext === 'xlsx' || ext === 'xls') return 'xlsx';
  if (ext === 'pptx' || ext === 'ppt') return 'pptx';
  if (['png', 'jpg', 'jpeg', 'gif', 'webp', 'bmp'].includes(ext)) return 'image';
  return 'other';
}

export type Extractor = (file: { path: string; name: string }) => Promise<string>;

export async function extractFileText(
  file: { path: string; name: string },
  extractors: Partial<Record<OfficeFileKind, Extractor>>,
  deps: { unsupportedError?: () => Error } = {},
): Promise<string> {
  const kind = classifyFile(file.name);
  const fn = extractors[kind];
  if (!fn) throw deps.unsupportedError?.() ?? new Error(`unsupported file type: ${kind}`);
  return fn(file);
}

export function extractPptxText(xml: string): string {
  return (xml.match(/<a:t>([\s\S]*?)<\/a:t>/g) ?? []).map(t => t.replace(/<\/?a:t>/g, '')).join('\n');
}

export async function extractPptx(
  buffer: ArrayBuffer,
  unzip: (b: ArrayBuffer) => Promise<{ file(name: string): Promise<string | null> }>,
): Promise<string> {
  const zip = await unzip(buffer);
  const out: string[] = [];
  for (let i = 1; i <= 50; i++) {
    const xml = await zip.file(`ppt/slides/slide${i}.xml`);
    if (xml != null) out.push(extractPptxText(xml));
  }
  return out.join('\n');
}
```

`packages/core/src/office/dropzone.ts`:
```ts
import { classifyFile, type OfficeFileKind } from './files';

export interface DropFile { name: string; path: string }
export interface DropDecision { attach: DropFile[]; copyToWorkspace: DropFile[] }

export function decideDrop(files: DropFile[], attachKinds: OfficeFileKind[] = ['image', 'pdf', 'docx', 'xlsx', 'pptx']): DropDecision {
  const attach: DropFile[] = [];
  const copyToWorkspace: DropFile[] = [];
  for (const f of files) {
    if (attachKinds.includes(classifyFile(f.name))) attach.push(f);
    else copyToWorkspace.push(f);
  }
  return { attach, copyToWorkspace };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/files.spec.ts src/office/dropzone.spec.ts`
Expected: PASS。

- [ ] **Step 5: main file IPC + DropZone 组件**

`apps/desktop/src/main/ipc/office.ts` 追加:
```ts
router.register('office.file.analyze', async (_e, path: string, name: string) => {
  const extractors = { docx: async () => (await import('mammoth')).extractRawText({ path }).then(r => r.value) } as never;
  const text = await extractFileText({ path, name }, extractors); // 实际实现补 pdfjs/xlsx/jszip 分支
  const result = await chatText(modelRouter, [{ role: 'user', content: `请分析下面文件内容并给出要点:\n\n${text.slice(0, 12000)}` }]);
  return { ok: true, result };
});
router.register('workspace.copyFiles', (_e, paths: string[]) => {
  for (const p of paths) copyFileSync(p, join(workspaceRoot(), basename(p)));
  return { ok: true };
});
```

`apps/desktop/src/renderer/src/components/office/DropZone.tsx`:
```tsx
import { useState } from 'react';
import { decideDrop } from '@jarvis/core';

export function DropZone({ children, onAttach, onCopied }: {
  children: React.ReactNode;
  onAttach: (files: Array<{ name: string; path: string }>) => void;
  onCopied: (names: string[]) => void;
}) {
  const [over, setOver] = useState(false);
  const onDrop = async (e: React.DragEvent) => {
    e.preventDefault();
    setOver(false);
    const files = [...e.dataTransfer.files].map(f => ({ name: f.name, path: (f as File & { path?: string }).path ?? f.name }));
    const { attach, copyToWorkspace } = decideDrop(files);
    if (copyToWorkspace.length) {
      await window.jarvis.invoke('workspace.copyFiles', copyToWorkspace.map(f => f.path));
      onCopied(copyToWorkspace.map(f => f.name));
    }
    if (attach.length) onAttach(attach);
  };
  return (
    <div data-testid="drop-zone" className={over ? 'drop-zone drop-zone--over' : 'drop-zone'}
      onDragOver={e => { e.preventDefault(); setOver(true); }} onDragLeave={() => setOver(false)} onDrop={e => void onDrop(e)}>
      {children}
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/office/DropZone.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { DropZone } from './DropZone';

describe('DropZone', () => {
  it('attaches image files and copies others to workspace', async () => {
    const invoke = vi.fn(async () => ({ ok: true }));
    (window as unknown as { jarvis: unknown }).jarvis = { invoke, onDidReceive: () => () => {} };
    const onAttach = vi.fn(); const onCopied = vi.fn();
    render(<DropZone onAttach={onAttach} onCopied={onCopied}><div>composer</div></DropZone>);
    const zone = screen.getByTestId('drop-zone');
    fireEvent.drop(zone, { dataTransfer: { files: [
      { name: 'a.png', path: '/tmp/a.png' },
      { name: 'notes.txt', path: '/tmp/notes.txt' }
    ] } });
    expect(onAttach).toHaveBeenCalledWith([expect.objectContaining({ name: 'a.png' })]);
    expect(invoke).toHaveBeenCalledWith('workspace.copyFiles', ['/tmp/notes.txt']);
  });
});
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/office/DropZone.spec.tsx`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/office/files.ts packages/core/src/office/files.spec.ts packages/core/src/office/dropzone.ts packages/core/src/office/dropzone.spec.ts apps/desktop/src/main/ipc/office.ts apps/desktop/src/renderer/src/components/office/DropZone.tsx apps/desktop/src/renderer/src/components/office/DropZone.spec.tsx
git commit -m "feat(office): file upload analysis and drag-drop handling (D12/L22)"
```

---

### Task 8: 多模态识图(L23)

**Files:**
- Create: `packages/core/src/office/content.ts`
- Create: `packages/core/src/office/content.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/office/ImageAttachment.tsx`
- Create: `apps/desktop/src/renderer/src/components/office/ImageAttachment.spec.tsx`

**Interfaces:**
- Consumes: M1 消息 content(M1 为 string,本任务扩展为 content[]);M1 adapters。
- Produces:
  - `MessageContent`(string | content[]);`toContentArray(content, imageUrls): MessageContent`
  - `isImageUrl(url): boolean`
  - `normalizeContent(content, adapter: 'openai'|'anthropic'): unknown` — 映射到各 API 消息格式。
  - main/chat:发送时若含图片,消息 content 走 content[];M1 adapters 增加 content[] 分支。
  - `ImageAttachment`:粘贴/拖拽图片 → data URL 预览 + 移除;发送时经 toContentArray 进 messages。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/content.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { toContentArray, isImageUrl, normalizeContent } from './content';

describe('multimodal content', () => {
  it('builds content array with text and images', () => {
    const c = toContentArray('hi', ['data:image/png;base64,AAA']);
    expect(Array.isArray(c)).toBe(true);
    if (Array.isArray(c)) {
      expect(c[0]).toEqual({ type: 'text', text: 'hi' });
      expect(c[1]).toEqual({ type: 'image_url', image_url: { url: 'data:image/png;base64,AAA' } });
    }
  });

  it('detects image urls', () => {
    expect(isImageUrl('data:image/png;base64,AAA')).toBe(true);
    expect(isImageUrl('https://x.com/a.png')).toBe(true);
    expect(isImageUrl('plain text')).toBe(false);
  });

  it('normalizes to openai and anthropic shapes', () => {
    const c = toContentArray('hi', ['data:image/png;base64,AAA']);
    const openai = normalizeContent(c, 'openai');
    const anth = normalizeContent(c, 'anthropic');
    expect(JSON.stringify(openai)).toContain('image_url');
    expect(JSON.stringify(anth)).toContain('image');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/content.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/content.ts`:
```ts
export type MessageContent =
  | string
  | Array<{ type: 'text'; text: string } | { type: 'image_url'; image_url: { url: string } }>;

export function toContentArray(content: string, imageUrls: string[]): MessageContent {
  const parts: Array<{ type: 'text'; text: string } | { type: 'image_url'; image_url: { url: string } }> = [];
  if (content) parts.push({ type: 'text', text: content });
  for (const u of imageUrls) parts.push({ type: 'image_url', image_url: { url: u } });
  return parts.length === 0 ? '' : parts;
}

export function isImageUrl(url: string): boolean {
  return /^data:image\//i.test(url) || /^https?:\/\/.+\.(png|jpe?g|gif|webp)(\?.*)?$/i.test(url);
}

export function normalizeContent(content: MessageContent, adapter: 'openai' | 'anthropic'): unknown {
  if (typeof content === 'string') return content;
  if (adapter === 'openai') {
    return content.map(p => p.type === 'image_url' ? { type: 'image_url', image_url: { url: p.image_url.url } } : { type: 'text', text: p.text });
  }
  // anthropic: image 走 content 块 { type:'image', source:{ type:'base64', media_type, data } }
  return content.map(p => p.type === 'image_url'
    ? { type: 'image', source: { type: 'base64', media_type: p.image_url.url.split(';')[0].replace('data:', ''), data: p.image_url.url.split(',')[1] ?? '' } }
    : { type: 'text', text: p.text });
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/content.spec.ts`
Expected: PASS。

- [ ] **Step 5: ImageAttachment + chat-store 接线**

`apps/desktop/src/renderer/src/components/office/ImageAttachment.tsx`:
```tsx
import { useState } from 'react';
import { isImageUrl } from '@jarvis/core';

export function ImageAttachment({ src, onRemove }: { src: string; onRemove: (src: string) => void }) {
  if (!isImageUrl(src)) return null;
  return (
    <span data-testid="image-attachment" className="image-attachment">
      <img src={src} alt="attachment" width={120} />
      <button data-testid="image-attachment-remove" onClick={() => onRemove(src)}>×</button>
    </span>
  );
}
```

聊天 store(M1 chat-store)扩展:state 增加 `pendingImages: string[]`(data URL);发送时:
```ts
const content = toContentArray(text, pendingImages);
// 经 main chat.send;M1 openai/anthropic adapter 处理 content 数组时调用 normalizeContent
```
`ImageAttachment.spec.tsx`:render + remove 回调。

- [ ] **Step 6: 运行测试确认通过**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/office/ImageAttachment.spec.tsx`
Expected: PASS。

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/office/content.ts packages/core/src/office/content.spec.ts apps/desktop/src/renderer/src/components/office/ImageAttachment.tsx apps/desktop/src/renderer/src/components/office/ImageAttachment.spec.tsx apps/desktop/src/renderer/src/stores/chat-store.ts packages/core/src/model/adapters
git commit -m "feat(office): multimodal image content and attachment UI (L23)"
```

---

### Task 9: Prompt 模板库(D15)

**Files:**
- Create: `packages/core/src/office/templates.ts`
- Create: `packages/core/src/office/templates.spec.ts`
- Create: `apps/desktop/src/main/ipc/templates.ts`
- Create: `apps/desktop/src/renderer/src/pages/PromptTemplatesPage.tsx`
- Create: `apps/desktop/src/renderer/src/pages/PromptTemplatesPage.spec.tsx`

**Interfaces:**
- Consumes: M0 schema(prompt_templates 表)。
- Produces:
  - `listTemplateVars(text): string[]`;`substituteTemplate(text, vars): string`
  - main `createTemplatesStore(db)`:list/create/update/remove;IPC `templates.list/create/update/delete/render`。
  - `PromptTemplatesPage`:模板列表 + 表单(name/正文/变量)+ 渲染预览 + "插入到输入框"。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/templates.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { listTemplateVars, substituteTemplate } from './templates';

describe('templates', () => {
  it('lists variables', () => {
    expect(listTemplateVars('Hi {{name}}, task {{ name }} done')).toEqual(['name']);
  });

  it('substitutes variables', () => {
    expect(substituteTemplate('Hi {{name}}', { name: 'Jarvis' })).toBe('Hi Jarvis');
    expect(substituteTemplate('No vars', {})).toBe('No vars');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/templates.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/templates.ts`:
```ts
export function listTemplateVars(text: string): string[] {
  const out: string[] = [];
  for (const m of text.matchAll(/\{\{\s*(\w+)\s*\}\}/g)) out.push(m[1]);
  return [...new Set(out)];
}

export function substituteTemplate(text: string, vars: Record<string, string>): string {
  return text.replace(/\{\{\s*(\w+)\s*\}\}/g, (_, k: string) => (k in vars ? vars[k] : ''));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/templates.spec.ts`
Expected: PASS。

- [ ] **Step 5: main store + IPC**

`apps/desktop/src/main/ipc/templates.ts`:
```ts
import type Database from 'better-sqlite3';
import { randomUUID } from 'node:crypto';

export function createTemplatesStore(db: Database.Database) {
  const list = () => (db.prepare('SELECT * FROM prompt_templates ORDER BY created_at').all() as Array<Record<string, unknown>>).map(r => ({
    id: r.id as string, name: r.name as string, text: r.text as string
  }));
  return {
    list,
    create(input: { name: string; text: string }) {
      db.prepare('INSERT INTO prompt_templates (id, name, text, created_at) VALUES (?,?,?,?)')
        .run(randomUUID(), input.name, input.text, new Date().toISOString());
      return list().at(-1)!;
    },
    update(id: string, input: { name?: string; text?: string }) {
      const cur = list().find(t => t.id === id);
      if (!cur) throw new Error(`template ${id} not found`);
      db.prepare('UPDATE prompt_templates SET name = ?, text = ? WHERE id = ?')
        .run(input.name ?? cur.name, input.text ?? cur.text, id);
    },
    remove(id: string) { db.prepare('DELETE FROM prompt_templates WHERE id = ?').run(id); }
  };
}
```

IpcRouter 注册 `templates.list/create/update/delete`、`templates.render { id, vars }`(substituteTemplate)。

- [ ] **Step 6: 页面 + 冒烟测试**

`apps/desktop/src/renderer/src/pages/PromptTemplatesPage.tsx`:
```tsx
import { useEffect, useState } from 'react';
import { listTemplateVars, substituteTemplate } from '@jarvis/core';

export function PromptTemplatesPage() {
  const [tpls, setTpls] = useState<Array<{ id: string; name: string; text: string }>>([]);
  const [name, setName] = useState('');
  const [text, setText] = useState('');
  const [preview, setPreview] = useState('');
  const refresh = async () => setTpls((await window.jarvis.invoke('templates.list')) as typeof tpls);
  useEffect(() => { void refresh(); }, []);
  const save = async () => { await window.jarvis.invoke('templates.create', { name, text }); setName(''); setText(''); await refresh(); };
  const renderPreview = async (id: string) => {
    const r = (await window.jarvis.invoke('templates.render', { id, vars: { name: 'Jarvis' } })) as { result: string };
    setPreview(r.result);
  };
  return (
    <div data-testid="prompt-templates">
      <h2>Prompt Templates</h2>
      <input data-testid="tpl-name" value={name} onChange={e => setName(e.target.value)} placeholder="name" />
      <textarea data-testid="tpl-text" value={text} onChange={e => setText(e.target.value)} placeholder="body with {{var}}" />
      <button data-testid="tpl-save" onClick={() => void save()}>+</button>
      <ul>{tpls.map(t => <li key={t.id}>{t.name} <button data-testid={`tpl-render-${t.id}`} onClick={() => void renderPreview(t.id)}>preview</button></li>)}</ul>
      <div data-testid="tpl-preview">{preview}</div>
    </div>
  );
}
```

`apps/desktop/src/renderer/src/pages/PromptTemplatesPage.spec.tsx`:
```tsx
import { describe, it, expect, beforeAll } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { PromptTemplatesPage } from './PromptTemplatesPage';

beforeAll(() => {
  (window as unknown as { jarvis: unknown }).jarvis = {
    invoke: async (m: string, a?: unknown) => m === 'templates.list' ? [{ id: 't1', name: 'review', text: 'Review {{name}}' }] : { result: 'Review Jarvis' },
    onDidReceive: () => () => {}
  };
});

describe('PromptTemplatesPage', () => {
  it('lists templates and renders preview', async () => {
    render(<PromptTemplatesPage />);
    await waitFor(() => expect(screen.getByTestId('tpl-save')).toBeTruthy());
    fireEvent.click(screen.getByTestId('tpl-render-t1'));
    await waitFor(() => expect(screen.getByTestId('tpl-preview').textContent).toBe('Review Jarvis'));
  });
});
```

- [ ] **Step 7: 运行测试确认通过 + i18n 对称**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/pages/PromptTemplatesPage.spec.tsx && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 8: Commit**

```bash
git add packages/core/src/office/templates.ts packages/core/src/office/templates.spec.ts apps/desktop/src/main/ipc/templates.ts apps/desktop/src/main/ipc/IpcRouter.ts apps/desktop/src/renderer/src/pages/PromptTemplatesPage.tsx apps/desktop/src/renderer/src/pages/PromptTemplatesPage.spec.tsx
git commit -m "feat(office): prompt template library with variable substitution (D15)"
```

---

### Task 10: 全局搜索(L21)+ 联网搜索源配置(L25)+ 办公聚合页

**Files:**
- Create: `packages/core/src/office/search.ts`
- Create: `packages/core/src/office/search.spec.ts`
- Create: `packages/core/src/office/searchProvider.ts`
- Create: `packages/core/src/office/searchProvider.spec.ts`
- Modify: `apps/desktop/src/main/db/migrations.ts`(追加 v3:FTS5)
- Create: `apps/desktop/src/main/ipc/search.ts`
- Create: `apps/desktop/src/renderer/src/components/office/SearchProvidersPage.tsx`
- Create: `apps/desktop/src/renderer/src/pages/OfficePage.tsx`
- Create: `apps/desktop/src/renderer/src/pages/OfficePage.spec.tsx`

**Interfaces:**
- Consumes: M1 web_search(M1 search.ts);M0 schema。
- Produces:
  - `ftsEscape(query)`;`rankFts(results, query)`。
  - `SearchProviderType`;`SearchProviderConfig { type; apiKey; enabled }`
  - `buildSearchRequest(provider, query, opts?): { url; headers; body? }`
  - `parseSearchResults(provider, body): Array<{ title; url; snippet }>`
  - main:`globalSearch(db, query)`(chat_messages/agents/tasks FTS MATCH → rankFts);IPC `search.global { query }`。
  - `settings.search_providers`(JSON);`web_search`(M1)改按配置路由;`SearchProvidersPage` 配置。
  - `OfficePage`:办公功能聚合(划词/写作/PDF/浏览器/文生图/语音入口)。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/office/search.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { ftsEscape, rankFts, type FtsRow } from './search';

describe('search', () => {
  it('escapes fts quotes', () => {
    expect(ftsEscape('say "hi"')).toBe('say ""hi""');
  });

  it('ranks title matches above snippet-only', () => {
    const rows: FtsRow[] = [
      { table: 'message', id: '1', title: 'irrelevant', snippet: 'contains jarvis keyword' },
      { table: 'message', id: '2', title: 'jarvis setup guide', snippet: 'steps' }
    ];
    const ranked = rankFts(rows, 'jarvis');
    expect(ranked[0].id).toBe('2');
  });
});
```

`packages/core/src/office/searchProvider.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildSearchRequest, parseSearchResults } from './searchProvider';

describe('search provider', () => {
  it('builds provider-specific requests', () => {
    const serper = buildSearchRequest({ type: 'serper', apiKey: 'k', enabled: true }, 'jarvis', { limit: 3 });
    expect(serper.url).toContain('serper');
    expect(serper.headers['X-API-KEY']).toBe('k');
    const brave = buildSearchRequest({ type: 'brave', apiKey: 'k2', enabled: true }, 'q');
    expect(brave.url).toContain('brave');
  });

  it('parses serper organic results', () => {
    const r = parseSearchResults('serper', { organic: [{ title: 'T', link: 'https://x', snippet: 'S' }] });
    expect(r[0]).toEqual({ title: 'T', url: 'https://x', snippet: 'S' });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/office/search.spec.ts src/office/searchProvider.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/office/search.ts`:
```ts
export interface FtsRow { table: string; id: string; title: string; snippet: string }

export function ftsEscape(query: string): string {
  return query.replace(/"/g, '""');
}

export function rankFts(results: FtsRow[], query: string): FtsRow[] {
  const q = query.toLowerCase();
  return results.map(r => ({
    r,
    score: (r.title.toLowerCase().includes(q) ? 10 : 0) + (r.snippet.toLowerCase().includes(q) ? 5 : 0)
  })).sort((a, b) => b.score - a.score).map(x => x.r);
}
```

`packages/core/src/office/searchProvider.ts`:
```ts
export type SearchProviderType = 'bing' | 'brave' | 'tavily' | 'serper';
export interface SearchProviderConfig { type: SearchProviderType; apiKey: string; enabled: boolean }
export interface SearchResultItem { title: string; url: string; snippet: string }
export interface SearchRequest { url: string; headers: Record<string, string>; body?: string }

export function buildSearchRequest(provider: SearchProviderConfig, query: string, opts: { limit?: number } = {}): SearchRequest {
  const limit = opts.limit ?? 5;
  switch (provider.type) {
    case 'tavily':
      return { url: 'https://api.tavily.com/search', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ api_key: provider.apiKey, query, max_results: limit }) };
    case 'serper':
      return { url: 'https://google.serper.dev/search', headers: { 'content-type': 'application/json', 'X-API-KEY': provider.apiKey }, body: JSON.stringify({ q: query, num: limit }) };
    case 'bing':
      return { url: `https://api.bing.microsoft.com/v7.0/search?q=${encodeURIComponent(query)}&count=${limit}`, headers: { 'Ocp-Apim-Subscription-Key': provider.apiKey } };
    case 'brave':
      return { url: `https://api.search.brave.com/res/v1/web/search?q=${encodeURIComponent(query)}`, headers: { 'X-Subscription-Token': provider.apiKey } };
  }
}

export function parseSearchResults(provider: SearchProviderType, body: unknown): SearchResultItem[] {
  const b = body as Record<string, unknown>;
  if (provider === 'serper') {
    return ((b.organic as Array<{ title?: string; link?: string; snippet?: string }>) ?? []).map(r => ({ title: r.title ?? '', url: r.link ?? '', snippet: r.snippet ?? '' }));
  }
  if (provider === 'tavily') {
    return ((b.results as Array<{ title?: string; url?: string; content?: string }>) ?? []).map(r => ({ title: r.title ?? '', url: r.url ?? '', snippet: r.content ?? '' }));
  }
  if (provider === 'bing') {
    return (((b.webPages as { value?: Array<{ name?: string; url?: string; snippet?: string }> }).value) ?? []).map(r => ({ title: r.name ?? '', url: r.url ?? '', snippet: r.snippet ?? '' }));
  }
  if (provider === 'brave') {
    return (((b.web as { results?: Array<{ title?: string; url?: string; description?: string }> }).results) ?? []).map(r => ({ title: r.title ?? '', url: r.url ?? '', snippet: r.description ?? '' }));
  }
  return [];
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/office/search.spec.ts src/office/searchProvider.spec.ts`
Expected: PASS。

- [ ] **Step 5: FTS5 迁移 + globalSearch IPC**

`apps/desktop/src/main/db/migrations.ts` 追加 v3:
```ts
{
  version: 3,
  sql: `
  CREATE VIRTUAL TABLE IF NOT EXISTS chat_messages_fts USING fts5(content);
  CREATE VIRTUAL TABLE IF NOT EXISTS agents_fts USING fts5(name, description);
  CREATE VIRTUAL TABLE IF NOT EXISTS tasks_fts USING fts5(title, description);
  CREATE TRIGGER IF NOT EXISTS chat_messages_fts_ai AFTER INSERT ON chat_messages BEGIN
    INSERT INTO chat_messages_fts(rowid, content) VALUES (new.rowid, new.content);
  END;
  CREATE TRIGGER IF NOT EXISTS chat_messages_fts_ad AFTER DELETE ON chat_messages BEGIN
    INSERT INTO chat_messages_fts(chat_messages_fts, rowid, content) VALUES ('delete', old.rowid, old.content);
  END;`
  // agents/tasks 触发器同构;既有数据回填:`INSERT INTO chat_messages_fts(rowid, content) SELECT rowid, content FROM chat_messages;`
}
```

`apps/desktop/src/main/ipc/search.ts`:
```ts
import { ftsEscape, rankFts } from '@jarvis/core';

export function globalSearch(db: Database.Database, query: string) {
  const q = `"${ftsEscape(query)}"`;
  const msgs = (db.prepare(`SELECT 'message' AS table, rowid AS id, '' AS title, content AS snippet FROM chat_messages_fts WHERE chat_messages_fts MATCH ? LIMIT 20`).all(q) as Array<Record<string, unknown>>).map(r => ({ table: r.table as string, id: String(r.id), title: r.title as string, snippet: r.snippet as string }));
  const agents = (db.prepare(`SELECT 'agent' AS table, rowid AS id, name AS title, description AS snippet FROM agents_fts WHERE agents_fts MATCH ? LIMIT 20`).all(q) as Array<Record<string, unknown>>).map(r => ({ table: r.table as string, id: String(r.id), title: r.title as string, snippet: r.snippet as string }));
  const tasks = (db.prepare(`SELECT 'task' AS table, rowid AS id, title AS title, description AS snippet FROM tasks_fts WHERE tasks_fts MATCH ? LIMIT 20`).all(q) as Array<Record<string, unknown>>).map(r => ({ table: r.table as string, id: String(r.id), title: r.title as string, snippet: r.snippet as string }));
  return rankFts([...msgs, ...agents, ...tasks], query);
}
```

- [ ] **Step 6: web_search 路由配置(L25)+ SearchProvidersPage**

`apps/desktop/src/main/ipc/search.ts` 追加:
```ts
export async function webSearch(settingsStore, query: string): Promise<SearchResultItem[]> {
  const configs = (settingsStore.get('search_providers') as SearchProviderConfig[] ?? []);
  const active = configs.find(c => c.enabled);
  if (!active) return legacyWebSearch(query); // M1 默认实现
  const req = buildSearchRequest(active, query);
  const res = await fetch(req.url, { method: req.body ? 'POST' : 'GET', headers: req.headers, body: req.body });
  return parseSearchResults(active.type, await res.json());
}
```

`SearchProvidersPage`:选择 type(bing/brave/tavily/serper)+ apiKey → 写 `settings.search_providers`。

- [ ] **Step 7: OfficePage 聚合 + 冒烟测试**

`apps/desktop/src/renderer/src/pages/OfficePage.tsx`(聚合入口,挂载 SelectionMenu 与各功能 Tab;冒烟渲染)。

`apps/desktop/src/renderer/src/pages/OfficePage.spec.tsx`:
```tsx
import { describe, it, expect, beforeAll } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import { getResources } from '@jarvis/i18n';
import { OfficePage } from './OfficePage';

beforeAll(async () => {
  await i18n.use(initReactI18next).init({ resources: getResources(), lng: 'zh-CN', ns: ['common'], defaultNS: 'common' });
  (window as unknown as { jarvis: unknown }).jarvis = { invoke: async () => [], onDidReceive: () => () => {} };
});

describe('OfficePage', () => {
  it('renders office entry', async () => {
    render(<OfficePage />);
    await waitFor(() => expect(screen.getByTestId('office-page')).toBeTruthy());
  });
});
```

- [ ] **Step 8: 运行测试确认通过 + i18n 对称**

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/pages/OfficePage.spec.tsx && cd packages/core && pnpm vitest run src/office && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 9: Commit**

```bash
git add packages/core/src/office/search.ts packages/core/src/office/search.spec.ts packages/core/src/office/searchProvider.ts packages/core/src/office/searchProvider.spec.ts apps/desktop/src/main/db/migrations.ts apps/desktop/src/main/ipc/search.ts apps/desktop/src/renderer/src/components/office/SearchProvidersPage.tsx apps/desktop/src/renderer/src/pages/OfficePage.tsx apps/desktop/src/renderer/src/pages/OfficePage.spec.tsx apps/desktop/src/main/ipc/tasks.ts
git commit -m "feat(office): global FTS search and configurable search providers (L21/L25)"
```

---

### M5 验收清单(Self-Review 对照)

**§21 M5 交付(M5 办公 | 划词/PDF/WebView/语音/识图 | D4-D12):**
- [x] D4 划词(翻译/解释/总结/搜索)— Task 1
- [x] D5 写作 — Task 2
- [x] D6 边写边译 — Task 2
- [x] D7 PDF 伴读(分页摘要/中英对照)— Task 3
- [x] D8 网页一键总结 — Task 4
- [x] I8 内置 WebView(会话隔离)— Task 4
- [x] D9 视频内容摘要 — Task 5
- [x] D10 文生图 — Task 5
- [x] D11 语音输入 — Task 6
- [x] D12 文件上传分析(PDF/Word/Excel/PPT)— Task 7
- [x] L22 拖拽 — Task 7
- [x] L23 多模态识图 — Task 8

**§10.6 M5 行(D4–D12,D15,I8,L21–L23,L25):**
- [x] D15 Prompt 模板库 — Task 9
- [x] L21 全局搜索(FTS5)— Task 10
- [x] L25 联网搜索源配置 — Task 10

**S4/S5 之外的 M5 验收口径:** 每条办公能力均有可运行入口(OfficePage 聚合)+ 核心逻辑单测通过;i18n 全对称;新增表(migration v3 FTS5)回填既有数据。

**M5 已知后置:** 系统级全局划词 Hook(A3/I1-I4,V2.0);Whisper 本地语音转写(可选,Web Speech 兜底);视频 transcript 自动获取(依赖第三方 API);D9 播放页跳转/Markdown 导出增强(M8);L24 TTS(明确排除)。
