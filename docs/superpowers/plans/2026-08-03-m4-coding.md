# M4 编程能力 (Coding) 实现计划 — 轻量编程 Agent

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. 本计划依赖 M0–M3:`packages/core`(AgentEngine/ToolRegistry/ModelRouter/ContextManager/Sandbox/ignore.ts)、main IPC(approval)、SQLite schema v1(mcp_servers/skills/tasks 表)、渲染层 store 模式、i18n 框架。
>
> **覆盖能力(§10.6 M4 行)**:E6–E11、E12、E14、E15、K3、K7、L26–L29(含 E1 代码索引,§21 M4 要求"索引/Git/@引用/LSP/Diff/Plan/并行/会话恢复")。Q3:B 已确认 — 无 Monaco,轻量代码面板为文件树 + 只读预览 + Diff。

**Goal:** 打通轻量编程闭环:Agent 在 Plan 模式(只读)或编辑模式下产出代码改动 → 文件树/只读预览 + 逐 hunk Diff Accept/Reject → 运行测试失败自动修复(E8)→ @引用与语义代码索引辅助理解 → Task 级一键回滚;并通过外部 IDE 协议对接与并行多 Task 扩展交付形态。

**Architecture:** 编程核心逻辑全部落在 `packages/core/src/coding/` 纯函数/类(便于单测):行级 diff、快照回滚、@引用解析、Plan 策略、结构化输出、headless LSP、修复循环、代码索引(embedding 依赖注入,测试用确定性 hashEmbedding,生产注入 Provider embedding)、文件树构建、会话恢复、并行执行。渲染层只做展示与交互(文件树、Diff 面板、MentionPicker、Plan 徽标、分屏);main 提供 IPC 与外部 IDE 桥(HTTP localhost + CLI)。L26 快照分 git-diff-patch 与 copy-on-write 两种,覆盖 git/非 git 工作区;TaskOrchestrator(M2)在 submit 前建快照、`task.rollback` 恢复。

**Tech Stack:** M0–M3 技术栈 + `node:http`/`child_process`(外部 IDE、LSP stdio)、自研行级 LCS diff(不引重型 diff 依赖)、`ajv`(L29 JSON Schema 校验)、`node:stream` PassThrough(测试流)。

## Global Constraints

(继承 M0–M3 全部约束。M4 相关复述:)

- **E10 Plan 模式:** `agent.plan_only = true` → 对模型暴露的工具列表过滤 write/shell/commit 工具,仅 read + analysis(含 `search_code`)可用;执行侧同样拦截(见 Task 4)。
- **E11/K3 轻量代码面板:** 无 Monaco(Q3:B);文件树 + 只读语法高亮预览 + unified Diff,逐 hunk Accept/Reject。
- **E12 外部 IDE:** HTTP localhost(17891)暴露 `/open?file=`、`/diff?task=`;CLI `jarvis open --file path:line` 调 `code -g`;VS Code 插件后置 V1.0。
- **E14 并行多 Task:** 每 Task 独立 workspace + 独立 Sandbox + 独立 MCP session;不共享运行时。
- **L26:** Task 开始前快照(git diff patch 或 copy-on-write);`task.rollback` 一键恢复并标记 Task failed。
- **L27:** 代码块 embedding 存本地 SQLite 表(`code_chunks`,embedding_json);生产可换 sqlite-vec/LanceDB;`embeddingFn` 依赖注入(单测用 hashEmbedding)。
- **L28:** 索引、文件树、Agent 访问共用 jarvisignore 过滤(M3 `sandbox/ignore.ts` 复用)。
- **L29:** ModelRouter 透传 `response_format: json_schema` + `ajv` 校验;校验失败抛 `StructuredOutputError` 由调用方重试。
- **每表单写者(§13.3):** `task_snapshots`/`code_chunks` 表归 main 属主(M2–M6 本地任务由 main 写;M7 起 Multica 路径由 daemon 写,见 M2/M3 Architecture 说明)。
- **E7 Agent 侧 LSP:** headless LSP client(stdio),服务于 Agent 自愈(E8),非用户 IDE 体验。
- **i18n:** M4 新增 UI(文件树/Diff/Plan/外部 IDE 菜单)须 zh-CN/en 对称。

## 文件结构总览(本里程碑新增)

```
packages/core/src/coding/
├── diff.ts                          # E9 行级 LCS diff + hunk 分组/解析/应用
├── diff.spec.ts
├── snapshot.ts                      # L26 Task 快照/回滚
├── snapshot.spec.ts
├── mention.ts                       # E6 @引用解析 + 上下文注入
├── mention.spec.ts
├── plan.ts                          # E10 plan_only 工具过滤
├── plan.spec.ts
├── structured.ts                    # L29 response_format json_schema + 校验
├── structured.spec.ts
├── lsp/LspClient.ts                 # E7 headless LSP stdio client
├── lsp/LspClient.spec.ts
├── fixloop.ts                       # E8 测试失败自动修复循环
├── fixloop.spec.ts
├── index/chunker.ts                 # L27 行块切分
├── index/IndexStore.ts              # L27 embedding 存储/检索 + search_code 工具
├── index/index.spec.ts
├── filetree.ts                      # E11 文件树构建(纯函数)
├── filetree.spec.ts
├── session.ts                       # E15 会话恢复(跨 Task 续接上下文)
├── session.spec.ts
├── parallel.ts                      # E14 并行执行(并发上限)
├── parallel.spec.ts
└── index.ts
apps/desktop/src/main/
├── db/migrations.ts                 # 追加 v2:task_snapshots / code_chunks 表
├── ipc/coding.ts                    # diff.applyAll / task.rollback / index.*
├── ipc/workspace.ts                 # workspace.tree / workspace.read
├── ipc/mention.ts                   # mention.search(fuzzy)
├── external/IdeBridge.ts            # E12 HTTP localhost + CLI open
├── external/IdeBridge.spec.ts
└── IpcRouter.ts                     # 注册新通道
apps/desktop/src/renderer/src/
├── components/coding/
│   ├── FileTree.tsx                 # E11 文件树(递归)
│   ├── FileTree.spec.tsx
│   ├── CodePreview.tsx              # E11 只读预览
│   ├── DiffPanel.tsx                # E9 逐 hunk Accept/Reject
│   ├── DiffPanel.spec.tsx
│   ├── MentionPicker.tsx            # E6 输入框 @ 触发
│   ├── MentionPicker.spec.tsx
│   ├── PlanModeBadge.tsx            # E10 状态徽标
│   └── SplitLayout.tsx              # K7 聊天 + 文件/Diff 分屏
├── pages/CodingPanelPage.tsx        # K3 代码面板入口
└── CodingPanelPage.spec.tsx
```

---

### Task 1: 行级 Diff 引擎(E9 基础)

**Files:**
- Create: `packages/core/src/coding/diff.ts`
- Create: `packages/core/src/coding/diff.spec.ts`

**Interfaces:**
- Consumes: 无。
- Produces:
  - `DiffLineType = 'context'|'add'|'del'`;`DiffLine { type; text }`
  - `Hunk { oldStart; oldLines; newStart; newLines; lines: DiffLine[] }`
  - `diffLines(a: string[], b: string[]): DiffLine[]` — 基于 LCS 的行级 diff(自研,不引重型依赖)。
  - `groupHunks(diff: DiffLine[], context?: number): Hunk[]` — 把 diff 聚成带上下文的 hunk。
  - `toUnified(hunks: Hunk[]): string` — 生成 `@@ -x,y +u,v @@` 文本。
  - `parseUnified(text: string): Hunk[]` — 解析回 hunk 列表。
  - `applyHunks(base: string[], hunks: Hunk[], accepts: boolean[]): string[]` — 逐 hunk accept(替换为 added)/reject(保留原 removed),内部跟踪 offset 保证后续 hunk 定位正确。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/coding/diff.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { diffLines, groupHunks, toUnified, parseUnified, applyHunks } from './diff';

const a = ['const x = 1;', 'export function add(a, b) {', '  return a + b;', '}'];
const b = ['const x = 2;', 'export function add(a, b) {', '  return a + b;', '  // new', '}'];

describe('diff engine', () => {
  it('produces add/del/context lines', () => {
    const d = diffLines(a, b);
    expect(d.some(l => l.type === 'del' && l.text === 'const x = 1;')).toBe(true);
    expect(d.some(l => l.type === 'add' && l.text === 'const x = 2;')).toBe(true);
  });

  it('groups hunks and roundtrips through unified text', () => {
    const hunks = groupHunks(diffLines(a, b));
    const reparsed = parseUnified(toUnified(hunks));
    expect(reparsed.length).toBe(hunks.length);
    expect(reparsed[0].oldStart).toBe(1);
    expect(reparsed[0].lines.filter(l => l.type === 'add').length).toBe(1);
  });

  it('applies accepted hunks to reach the target file', () => {
    const hunks = groupHunks(diffLines(a, b));
    const accepts = hunks.map(() => true);
    expect(applyHunks(a, hunks, accepts).join('\n')).toBe(b.join('\n'));
  });

  it('keeps original when all hunks rejected', () => {
    const hunks = groupHunks(diffLines(a, b));
    const rejects = hunks.map(() => false);
    expect(applyHunks(a, hunks, rejects).join('\n')).toBe(a.join('\n'));
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/diff.spec.ts`
Expected: FAIL(`./diff` 不存在)。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/diff.ts`:
```ts
export type DiffLineType = 'context' | 'add' | 'del';
export interface DiffLine { type: DiffLineType; text: string }
export interface Hunk { oldStart: number; oldLines: number; newStart: number; newLines: number; lines: DiffLine[] }

export function diffLines(a: string[], b: string[]): DiffLine[] {
  const n = a.length, m = b.length;
  const dp: number[][] = Array.from({ length: n + 1 }, () => new Array<number>(m + 1).fill(0));
  for (let i = n - 1; i >= 0; i--)
    for (let j = m - 1; j >= 0; j--)
      dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]);
  const out: DiffLine[] = [];
  let i = 0, j = 0;
  while (i < n && j < m) {
    if (a[i] === b[j]) { out.push({ type: 'context', text: a[i] }); i++; j++; }
    else if (dp[i + 1][j] >= dp[i][j + 1]) { out.push({ type: 'del', text: a[i] }); i++; }
    else { out.push({ type: 'add', text: b[j] }); j++; }
  }
  while (i < n) { out.push({ type: 'del', text: a[i] }); i++; }
  while (j < m) { out.push({ type: 'add', text: b[j] }); j++; }
  return out;
}

export function groupHunks(diff: DiffLine[], context = 3): Hunk[] {
  const hunks: Hunk[] = [];
  let oldPos = 1, newPos = 1;
  const n = diff.length;
  let i = 0;
  while (i < n) {
    if (diff[i].type === 'context') { oldPos++; newPos++; i++; continue; }
    let j = i;
    while (j < n && diff[j].type !== 'context') j++; // change run 结束(含上下文前的位置)
    let cb = 0;
    for (let k = i - 1; k >= 0 && diff[k].type === 'context' && cb < context; k--) cb++;
    const hunkStart = i - cb;
    let ca = 0;
    for (let l = j; l < n && diff[l].type === 'context' && ca < context; l++) ca++;
    const hunkEnd = j + ca;
    const lines = diff.slice(hunkStart, hunkEnd);
    const oldLines = lines.filter(x => x.type !== 'add').length;
    const newLines = lines.filter(x => x.type !== 'del').length;
    hunks.push({ oldStart: oldPos - cb, oldLines, newStart: newPos - cb, newLines, lines });
    for (let p = i; p < hunkEnd; p++) { if (diff[p].type !== 'add') oldPos++; if (diff[p].type !== 'del') newPos++; }
    i = hunkEnd;
  }
  return hunks;
}

export function toUnified(hunks: Hunk[]): string {
  const out: string[] = [];
  for (const h of hunks) {
    out.push(`@@ -${h.oldStart},${h.oldLines} +${h.newStart},${h.newLines} @@`);
    for (const l of h.lines) out.push(`${l.type === 'add' ? '+' : l.type === 'del' ? '-' : ' '}${l.text}`);
  }
  return out.join('\n');
}

export function parseUnified(text: string): Hunk[] {
  const hunks: Hunk[] = [];
  let cur: Hunk | null = null;
  for (const line of text.split('\n')) {
    const m = /^@@ -(\d+),(\d+) \+(\d+),(\d+) @@/.exec(line);
    if (m) {
      if (cur) hunks.push(cur);
      cur = { oldStart: +m[1], oldLines: +m[2], newStart: +m[3], newLines: +m[4], lines: [] };
      continue;
    }
    if (!cur) continue;
    const type: DiffLineType = line.startsWith('+') ? 'add' : line.startsWith('-') ? 'del' : 'context';
    cur.lines.push({ type, text: line.slice(1) });
  }
  if (cur) hunks.push(cur);
  return hunks;
}

export function applyHunks(base: string[], hunks: Hunk[], accepts: boolean[]): string[] {
  let out = base;
  let offset = 0;
  for (let i = 0; i < hunks.length; i++) {
    const h = hunks[i];
    const start = h.oldStart - 1 + offset;
    const removed = h.lines.filter(l => l.type === 'del').map(l => l.text);
    const added = h.lines.filter(l => l.type === 'add').map(l => l.text);
    out = [...out.slice(0, start), ...(accepts[i] ? added : removed), ...out.slice(start + h.oldLines)];
    offset += (accepts[i] ? added.length : removed.length) - h.oldLines;
  }
  return out;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/diff.spec.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/coding/diff.ts packages/core/src/coding/diff.spec.ts
git commit -m "feat(core): line-based unified diff engine with per-hunk apply (E9)"
```

---

### Task 2: TaskSnapshot / Task 级回滚(L26)+ schema v2

**Files:**
- Create: `packages/core/src/coding/snapshot.ts`
- Create: `packages/core/src/coding/snapshot.spec.ts`
- Modify: `apps/desktop/src/main/db/migrations.ts`(追加 v2)
- Create: `apps/desktop/src/main/ipc/coding.ts`(task_snapshot store + task.rollback)

**Interfaces:**
- Consumes: M2 TaskOrchestrator/TaskStoreAdapter;M0 MIGRATIONS 机制。
- Produces:
  - `SnapshotGit { exec(args: string[], cwd: string): Promise<{ stdout; stderr }> }`
  - `SnapshotFs { exists(p): boolean; read(p): string|null; write(p, c): void; mkdir(p): void; walk(p): string[] }`
  - `SnapshotMeta = { kind:'git'; patchFile:string } | { kind:'copy'; dir:string; files:Record<string,string> }`
  - `SnapshotStore { save(taskId, meta): void; get(taskId): SnapshotMeta|null }`
  - `createTaskSnapshot(opts): Promise<void>` — git 工作区用 `git diff` 存 patch 到 `.jarvis/snapshots/{taskId}.patch`;非 git 用 copy-on-write 拷贝工作区(跳过 `.jarvis/`、`node_modules/`)到 `.jarvis/snapshots/{taskId}/`。
  - `restoreSnapshot(opts): Promise<void>` — git: `git apply -R <patchFile>`;copy: 把快照文件写回工作区。
  - `SnapshotError`
  - main:`createSnapshotStore(db)` 读写 `task_snapshots` 表;IPC `task.rollback { taskId }`。

- [ ] **Step 1: 追加 v2 迁移**

`apps/desktop/src/main/db/migrations.ts` 的 `MIGRATIONS` 数组末尾追加:
```ts
{
  version: 2,
  sql: `CREATE TABLE IF NOT EXISTS task_snapshots (
    id TEXT PRIMARY KEY, task_id TEXT NOT NULL, kind TEXT NOT NULL,
    meta_json TEXT NOT NULL, created_at TEXT NOT NULL
  );
  CREATE INDEX IF NOT EXISTS idx_task_snapshots_task ON task_snapshots(task_id);
  CREATE TABLE IF NOT EXISTS code_chunks (
    id TEXT PRIMARY KEY, path TEXT NOT NULL, start_line INTEGER NOT NULL,
    end_line INTEGER NOT NULL, text TEXT NOT NULL, embedding_json TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  CREATE INDEX IF NOT EXISTS idx_code_chunks_path ON code_chunks(path);`
}
```

- [ ] **Step 2: 编写失败测试**

`packages/core/src/coding/snapshot.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { createTaskSnapshot, restoreSnapshot, type SnapshotGit, type SnapshotFs, type SnapshotMeta } from './snapshot';

class MemFs implements SnapshotFs {
  files = new Map<string, string>();
  exists(p: string) { return this.files.has(p); }
  read(p: string) { return this.files.get(p) ?? null; }
  write(p: string, c: string) { this.files.set(p, c); }
  mkdir(p: string) { this.files.set(p + '/', ''); }
  walk(p: string) { return [...this.files.keys()].filter(k => k.startsWith(p) && !k.endsWith('/')); }
}

describe('TaskSnapshot', () => {
  const store = new Map<string, SnapshotMeta>();

  it('snapshots a git workspace as a patch and restores via reverse apply', async () => {
    const git: SnapshotGit = { exec: async (args) => ({ stdout: args[0] === 'diff' ? 'patch-data' : '', stderr: '' }) };
    const fs = new MemFs();
    fs.write('/ws/a.txt', 'v1');
    await createTaskSnapshot({ taskId: 't1', workspaceRoot: '/ws', git, fs, store, isGitRepo: true });
    expect(fs.read('/ws/.jarvis/snapshots/t1.patch')).toBe('patch-data');
    expect(store.get('t1')?.kind).toBe('git');
    await restoreSnapshot({ taskId: 't1', workspaceRoot: '/ws', git, fs, store });
    expect(git.exec).not.toThrow();
  });

  it('copy-on-write snapshots non-git workspaces and skips node_modules', async () => {
    const git: SnapshotGit = { exec: async () => ({ stdout: '', stderr: '' }) };
    const fs = new MemFs();
    fs.write('/ws/a.txt', 'v1');
    fs.write('/ws/node_modules/x.js', 'big');
    await createTaskSnapshot({ taskId: 't2', workspaceRoot: '/ws', git, fs, store, isGitRepo: false });
    const meta = store.get('t2')!;
    expect(meta.kind).toBe('copy');
    if (meta.kind === 'copy') {
      expect(meta.files['a.txt']).toBe('v1');
      expect(meta.files['node_modules/x.js']).toBeUndefined();
      fs.write('/ws/a.txt', 'v2'); // 改动后回滚
      await restoreSnapshot({ taskId: 't2', workspaceRoot: '/ws', git, fs, store });
      expect(fs.read('/ws/a.txt')).toBe('v1');
    }
  });

  it('throws when restoring a task with no snapshot', async () => {
    await expect(restoreSnapshot({ taskId: 'nope', workspaceRoot: '/ws', git: { exec: async () => ({ stdout: '', stderr: '' }) }, fs: new MemFs(), store })).rejects.toThrow();
  });
});
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/snapshot.spec.ts`
Expected: FAIL。

- [ ] **Step 4: 编写实现**

`packages/core/src/coding/snapshot.ts`:
```ts
export interface SnapshotGit { exec(args: string[], cwd: string): Promise<{ stdout: string; stderr: string }> }
export interface SnapshotFs {
  exists(p: string): boolean;
  read(p: string): string | null;
  write(p: string, c: string): void;
  mkdir(p: string): void;
  walk(p: string): string[];
}
export type SnapshotMeta =
  | { kind: 'git'; patchFile: string }
  | { kind: 'copy'; dir: string; files: Record<string, string> };
export interface SnapshotStore { save(taskId: string, meta: SnapshotMeta): void; get(taskId: string): SnapshotMeta | null }

export class SnapshotError extends Error {}

export const SNAPSHOT_DIR = '.jarvis/snapshots';

export interface CreateSnapshotOpts {
  taskId: string; workspaceRoot: string; git: SnapshotGit; fs: SnapshotFs;
  store: SnapshotStore; isGitRepo: boolean;
}

export async function createTaskSnapshot(opts: CreateSnapshotOpts): Promise<void> {
  const { taskId, workspaceRoot, git, fs, store, isGitRepo } = opts;
  const snapDir = `${workspaceRoot}/${SNAPSHOT_DIR}`;
  if (isGitRepo) {
    const { stdout } = await git.exec(['diff'], workspaceRoot);
    fs.mkdir(snapDir);
    const patchFile = `${snapDir}/${taskId}.patch`;
    fs.write(patchFile, stdout);
    store.save(taskId, { kind: 'git', patchFile });
  } else {
    const dir = `${snapDir}/${taskId}`;
    fs.mkdir(dir);
    const files: Record<string, string> = {};
    for (const p of fs.walk(workspaceRoot)) {
      const rel = p.slice(workspaceRoot.length + 1);
      if (rel.startsWith('.jarvis/') || rel.startsWith('node_modules/')) continue;
      const c = fs.read(p);
      if (c === null) continue;
      files[rel] = c;
      fs.write(`${dir}/${rel}`, c);
    }
    store.save(taskId, { kind: 'copy', dir, files });
  }
}

export async function restoreSnapshot(opts: Omit<CreateSnapshotOpts, 'isGitRepo'>): Promise<void> {
  const meta = opts.store.get(opts.taskId);
  if (!meta) throw new SnapshotError(`no snapshot for task ${opts.taskId}`);
  if (meta.kind === 'git') {
    const { stderr } = await opts.git.exec(['apply', '-R', meta.patchFile], opts.workspaceRoot);
    if (stderr) throw new SnapshotError(stderr);
  } else {
    for (const [rel, content] of Object.entries(meta.files)) {
      opts.fs.write(`${opts.workspaceRoot}/${rel}`, content);
    }
  }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/snapshot.spec.ts`
Expected: PASS。

- [ ] **Step 6: main 接线 — createSnapshotStore + task.rollback**

`apps/desktop/src/main/ipc/coding.ts`:
```ts
import type Database from 'better-sqlite3';
import { randomUUID } from 'node:crypto';
import { createTaskSnapshot, restoreSnapshot, type SnapshotGit, type SnapshotFs, type SnapshotMeta } from '@jarvis/core';
import { existsSync, readdirSync, readFileSync, writeFileSync, mkdirSync } from 'node:fs';
import { join } from 'node:path';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';

const exec = promisify(execFile);

export function createSnapshotStore(db: Database.Database): { save: (t: string, m: SnapshotMeta) => void; get: (t: string) => SnapshotMeta | null } {
  const insert = db.prepare('INSERT INTO task_snapshots (id, task_id, kind, meta_json, created_at) VALUES (?,?,?,?,?)');
  const select = db.prepare('SELECT kind, meta_json FROM task_snapshots WHERE task_id = ?');
  return {
    save(taskId, meta) { insert.run(randomUUID(), taskId, meta.kind, JSON.stringify(meta), new Date().toISOString()); },
    get(taskId) { const r = select.get(taskId) as { kind: string; meta_json: string } | undefined; return r ? JSON.parse(r.meta_json) as SnapshotMeta : null; }
  };
}

export async function snapshotBeforeTask(wsRoot: string, taskId: string, store: { save: (t: string, m: SnapshotMeta) => void }): Promise<void> {
  const git: SnapshotGit = { exec: async (args, cwd) => { const { stdout, stderr } = await exec('git', args, { cwd }); return { stdout, stderr }; } };
  const fsImpl: SnapshotFs = {
    exists: existsSync, read: (p) => { try { return readFileSync(p, 'utf8'); } catch { return null; } },
    write: (p, c) => { mkdirSync(join(p, '..'), { recursive: true }); writeFileSync(p, c); },
    mkdir: (p) => mkdirSync(p, { recursive: true }),
    walk: (p) => { const out: string[] = []; const walk = (d: string) => { for (const e of readdirSync(d, { withFileTypes: true })) { const f = join(d, e.name); if (e.isDirectory()) walk(f); else out.push(f); } }; walk(p); return out; }
  };
  const isGitRepo = existsSync(join(wsRoot, '.git'));
  await createTaskSnapshot({ taskId, workspaceRoot: wsRoot, git, fs: fsImpl, store, isGitRepo });
}
```

在 `TaskOrchestrator`(M2 main/tasks.ts)的 submit 流程中、engine 开始前调用 `snapshotBeforeTask(ws, taskId, snapshotStore)`;注册 IPC:
```ts
this.register('task.rollback', async (_e, taskId: string) => {
  const task = getTask(taskId);
  await restoreSnapshot({ taskId, workspaceRoot: task.workspaceId, git: ..., fs: fsImpl, store: snapshotStore });
  markTask(taskId, 'failed', { reason: 'rolled_back' });
  return { ok: true };
});
```
(还原后 Task 标记 failed,并推送 `task:status` 事件。)

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/coding/snapshot.ts packages/core/src/coding/snapshot.spec.ts apps/desktop/src/main/db/migrations.ts apps/desktop/src/main/ipc/coding.ts apps/desktop/src/main/ipc/tasks.ts apps/desktop/src/main/ipc/IpcRouter.ts
git commit -m "feat(coding): task-level snapshot and rollback with schema v2 (L26)"
```

---

### Task 3: @ 引用(E6)解析与上下文注入

**Files:**
- Create: `packages/core/src/coding/mention.ts`
- Create: `packages/core/src/coding/mention.spec.ts`

**Interfaces:**
- Consumes: 无(读文件依赖注入)。
- Produces:
  - `Mention { raw; query }`;`parseMentions(text: string): Mention[]` — 正则 `(?:^|\s)@([^\s@#]+)`。
  - `MentionKind = 'file'|'folder'|'symbol'|'doc'|'agent'`
  - `MentionCandidate { id; label; kind; path? }`
  - `ContextAttachment { type: MentionKind|'text'; source; content }`
  - `MentionResolver { search(query): Promise<MentionCandidate[]>; resolve(candidate): Promise<ContextAttachment> }`
  - `resolveFileMention(query, workspaceRoot, readImpl): ContextAttachment` — 把 `@src/a.ts` 解析为文件内容附件。
  - `buildMentionBlock(attachments: ContextAttachment[]): string` — 生成可拼入 messages 的 `<referenced>` 块。
  - `MentionError`

- [ ] **Step 1: 编写失败测试**

`packages/core/src/coding/mention.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseMentions, resolveFileMention, buildMentionBlock } from './mention';

describe('mention', () => {
  it('parses @file mentions from text', () => {
    const m = parseMentions('请阅读 @src/a.ts 并参考 @docs/guide.md 摘要');
    expect(m.map(x => x.query)).toEqual(['src/a.ts', 'docs/guide.md']);
  });

  it('resolves a file mention to a content attachment', () => {
    const files = new Map([['/ws/src/a.ts', 'export const x = 1;']]);
    const att = resolveFileMention('src/a.ts', '/ws', (p) => files.get(p) ?? null);
    expect(att.type).toBe('file');
    expect(att.content).toContain('export const x');
  });

  it('throws on missing file', () => {
    expect(() => resolveFileMention('nope.ts', '/ws', () => null)).toThrow('not found');
  });

  it('builds an injection block with sources', () => {
    const block = buildMentionBlock([{ type: 'file', source: 'src/a.ts', content: 'export const x = 1;' }]);
    expect(block).toContain('<referenced>');
    expect(block).toContain('src/a.ts');
    expect(buildMentionBlock([])).toBe('');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/mention.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/mention.ts`:
```ts
export interface Mention { raw: string; query: string }
export type MentionKind = 'file' | 'folder' | 'symbol' | 'doc' | 'agent';
export interface MentionCandidate { id: string; label: string; kind: MentionKind; path?: string }
export interface ContextAttachment { type: MentionKind | 'text'; source: string; content: string }
export interface MentionResolver {
  search(query: string): Promise<MentionCandidate[]>;
  resolve(candidate: MentionCandidate): Promise<ContextAttachment>;
}

export class MentionError extends Error {}

export function parseMentions(text: string): Mention[] {
  const out: Mention[] = [];
  const re = /(?:^|\s)@([^\s@#]+)/g;
  let m: RegExpExecArray | null;
  while ((m = re.exec(text))) out.push({ raw: m[0].trim(), query: m[1] });
  return out;
}

export function resolveFileMention(query: string, workspaceRoot: string, readImpl: (p: string) => string | null): ContextAttachment {
  const path = `${workspaceRoot}/${query}`;
  const content = readImpl(path);
  if (content === null) throw new MentionError(`not found: ${query}`);
  return { type: 'file', source: query, content };
}

export function buildMentionBlock(attachments: ContextAttachment[]): string {
  if (attachments.length === 0) return '';
  return '\n<referenced>\n' + attachments.map(a => `[${a.type}] ${a.source}\n${a.content}`).join('\n---\n') + '\n</referenced>';
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/mention.spec.ts`
Expected: PASS。

- [ ] **Step 5: 注入到消息构建(修改 main/tasks.ts 的 buildTaskMessages)**

```ts
import { parseMentions, resolveFileMention, buildMentionBlock } from '@jarvis/core';
function attachMentions(userInput: string, wsRoot: string): { input: string; block: string } {
  const refs = parseMentions(userInput).map(m => resolveFileMention(m.query, wsRoot, readImpl));
  return { input: userInput.replace(/@([^\s@#]+)/g, '').replace(/\s+/g, ' ').trim(), block: buildMentionBlock(refs) };
}
// 组装时: user = `${input}${block}`
```

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/coding/mention.ts packages/core/src/coding/mention.spec.ts packages/core/src/coding/index.ts apps/desktop/src/main/ipc/tasks.ts
git commit -m "feat(core): @mention parse and context attachment injection (E6)"
```

---

### Task 4: Plan 模式(E10)+ 结构化输出(L29)

**Files:**
- Create: `packages/core/src/coding/plan.ts`
- Create: `packages/core/src/coding/plan.spec.ts`
- Create: `packages/core/src/coding/structured.ts`
- Create: `packages/core/src/coding/structured.spec.ts`

**Interfaces:**
- Consumes: M2 AgentEngine(工具列表来源);M1 ModelRouter/ChatChunk。
- Produces:
  - `isPlanBlocked(toolName): boolean` — 命中 write/shell/commit/MCP 类正则即被 Plan 拦截。
  - `planVisibleTools(all: string[], planEnabled: boolean): string[]` — Plan 开启时过滤被拦截工具。
  - main:engine 构造时按 `agent.plan_only` 过滤工具列表并在 approvalGate 前二次拦截(见 Step 4)。
  - `StructuredOutputError`
  - `structuredChat(chatFn, req, validate?): Promise<unknown>` — 透传 `response_format: { type:'json_schema', json_schema:{ name:'result', schema } }`,收集流式 `deltaText` 拼 JSON,`JSON.parse` 后 `ajv` 校验(依赖注入 validate,默认通过)。

- [ ] **Step 1: 编写 Plan 失败测试**

`packages/core/src/coding/plan.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { isPlanBlocked, planVisibleTools } from './plan';

const ALL = ['read_file', 'list_dir', 'write_file', 'run_shell', 'git_status', 'git_commit', 'search_code', 'mcp:fs:read'];

describe('plan mode', () => {
  it('blocks write/shell/commit/mcp tools', () => {
    expect(isPlanBlocked('write_file')).toBe(true);
    expect(isPlanBlocked('run_shell')).toBe(true);
    expect(isPlanBlocked('git_commit')).toBe(true);
    expect(isPlanBlocked('mcp:fs:read')).toBe(true);
    expect(isPlanBlocked('read_file')).toBe(false);
    expect(isPlanBlocked('git_status')).toBe(false);
    expect(isPlanBlocked('search_code')).toBe(false);
  });

  it('filters tools when plan enabled, keeps all when disabled', () => {
    const visible = planVisibleTools(ALL, true);
    expect(visible).toContain('read_file');
    expect(visible).toContain('search_code');
    expect(visible).not.toContain('write_file');
    expect(visible).not.toContain('run_shell');
    expect(planVisibleTools(ALL, false)).toHaveLength(ALL.length);
  });
});
```

- [ ] **Step 2: 编写实现与测试通过**

`packages/core/src/coding/plan.ts`:
```ts
const BLOCKED = /^(write_file|run_shell|git_add|git_commit|git_branch|mcp:|.*_write$|.*_commit$|.*_delete$)/;
export function isPlanBlocked(toolName: string): boolean {
  return BLOCKED.test(toolName);
}
export function planVisibleTools(all: string[], planEnabled: boolean): string[] {
  return planEnabled ? all.filter(t => !isPlanBlocked(t)) : all;
}
```

Run: `cd packages/core && pnpm vitest run src/coding/plan.spec.ts`
Expected: PASS。

- [ ] **Step 3: 编写结构化输出失败测试**

`packages/core/src/coding/structured.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { structuredChat, StructuredOutputError } from './structured';

async function* fakeChat(req: { messages: unknown[]; model: string; provider: unknown; response_format?: unknown }): AsyncIterable<{ deltaText: string }> {
  if (req.response_format) yield { deltaText: '{"ok":true,"count":3}' };
  else yield { deltaText: 'not json {' };
}

describe('structuredChat', () => {
  it('collects chunks, parses JSON, validates', async () => {
    const out = await structuredChat(fakeChat, {
      messages: [{ role: 'user', content: 'x' }], model: 'm', provider: { type: 'openai', id: 'p' },
      schema: { type: 'object', properties: { ok: { type: 'boolean' } }, required: ['ok'] }
    }, (o) => { const v = o as { ok?: boolean }; return v.ok ? null : 'missing ok'; });
    expect(out).toEqual({ ok: true, count: 3 });
  });

  it('throws StructuredOutputError on invalid JSON', async () => {
    await expect(structuredChat(fakeChat, { messages: [], model: 'm', provider: { type: 'openai', id: 'p' }, schema: {} }, () => null))
      .rejects.toThrow(StructuredOutputError);
  });
});
```

- [ ] **Step 4: 编写实现并接线 Plan 过滤**

`packages/core/src/coding/structured.ts`:
```ts
export class StructuredOutputError extends Error {}

export interface StructuredChatRequest {
  messages: Array<{ role: string; content: string }>;
  model: string;
  provider: { type: string; id: string };
  schema: object;
}

export type ChatFn = (req: {
  messages: unknown[]; model: string; provider: unknown;
  response_format: { type: 'json_schema'; json_schema: { name: string; schema: object } };
}) => AsyncIterable<{ deltaText?: string }>;

export async function structuredChat(
  chatFn: ChatFn,
  req: StructuredChatRequest,
  validate: (obj: unknown) => string | null = () => null,
): Promise<unknown> {
  const chunks: string[] = [];
  for await (const c of chatFn({
    messages: req.messages, model: req.model, provider: req.provider,
    response_format: { type: 'json_schema', json_schema: { name: 'result', schema: req.schema } }
  })) {
    chunks.push(c.deltaText ?? '');
  }
  const text = chunks.join('').trim();
  let obj: unknown;
  try { obj = JSON.parse(text); } catch { throw new StructuredOutputError(`invalid JSON output: ${text}`); }
  const err = validate(obj);
  if (err) throw new StructuredOutputError(err);
  return obj;
}
```

main/tasks.ts 接线(engine 构造处):
```ts
const visible = planVisibleTools(registry.list().map(t => t.name), agent.planOnly);
engine.setVisibleTools(visible); // AgentEngine 只向模型暴露 visible;执行时同名单再拦截
// approvalGate 前: if (agent.planOnly && isPlanBlocked(toolName)) return false;
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/structured.spec.ts`
Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/coding/plan.ts packages/core/src/coding/plan.spec.ts packages/core/src/coding/structured.ts packages/core/src/coding/structured.spec.ts packages/core/src/coding/index.ts apps/desktop/src/main/ipc/tasks.ts
git commit -m "feat(core): plan-only tool filtering and structured JSON output (E10/L29)"
```

---

### Task 5: LSP Agent 侧(E7)+ 测试运行与失败修复循环(E8)

**Files:**
- Create: `packages/core/src/coding/lsp/LspClient.ts`
- Create: `packages/core/src/coding/lsp/LspClient.spec.ts`
- Create: `packages/core/src/coding/fixloop.ts`
- Create: `packages/core/src/coding/fixloop.spec.ts`

**Interfaces:**
- Consumes: M3 Sandbox(命令白名单);M2 ToolRegistry。
- Produces:
  - `LspClient { initialize(): Promise<void>; didOpen(uri, text): void; didChange(uri, text, version): void; pullDiagnostics(uri): LspDiagnostic[]; shutdown(): void }` — 基于 Content-Length 帧 + JSON-RPC 的 stdio LSP client;`textDocument/publishDiagnostics` 通知缓存到 `diagnostics`。
  - `LspDiagnostic { line; character; endLine; endCharacter; severity; message }`
  - `registerRunTestsTool(registry, sandbox, deps)` — `run_tests` 工具,command 默认空(用项目默认测试命令)。
  - `runTestFixLoop(deps, opts)` — 有界重试:runTests → 失败则 pullDiagnostics + runEngine(feedback) → 重复,直到 pass 或重试耗尽。
  - `TestRunResult { pass; output }`;`FixLoopDeps { runTests(); pullDiagnostics(); runEngine(feedback) }`

- [ ] **Step 1: 编写 LSP 失败测试(用 PassThrough 双工流)**

`packages/core/src/coding/lsp/LspClient.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { PassThrough } from 'node:stream';
import { LspClient, type LspClientDeps } from './LspClient';

interface FakeProcLike { stdout: PassThrough; stdin: { write(d: string): void; end(): void }; kill(): void }

function frame(msg: unknown): Buffer {
  const body = JSON.stringify(msg);
  return Buffer.from(`Content-Length: ${Buffer.byteLength(body)}\r\n\r\n${body}`);
}

function makeFakeProc(): FakeProcLike {
  const stdout = new PassThrough();
  const stdin = {
    write(d: string) {
      const body = d.slice(d.indexOf('\r\n\r\n') + 4);
      const msg = JSON.parse(body);
      if (msg.method === 'initialize') { const id = msg.id as number; stdout.write(frame({ jsonrpc: '2.0', id, result: { capabilities: {} } })); }
      if (msg.method === 'textDocument/didOpen') {
        const uri = (msg.params as { textDocument: { uri: string } }).textDocument.uri;
        setImmediate(() => stdout.write(frame({ jsonrpc: '2.0', method: 'textDocument/publishDiagnostics', params: { uri, diagnostics: [{ range: { start: { line: 0, character: 0 }, end: { line: 0, character: 1 } }, message: 'TS2322 err' }] } })));
      }
    },
    end() {}
  };
  return { stdout, stdin, kill() {} };
}

describe('LspClient', () => {
  it('initializes and collects publishDiagnostics for an opened doc', async () => {
    const proc = makeFakeProc();
    const deps: LspClientDeps = { spawnImpl: () => proc as unknown as import('node:child_process').ChildProcess };
    const lsp = new LspClient('/ws', 'tsserver', ['--stdio'], deps);
    await lsp.initialize();
    lsp.didOpen('file:///ws/a.ts', 'const x: string = 1;');
    await new Promise(r => setImmediate(r));
    const diags = lsp.pullDiagnostics('file:///ws/a.ts');
    expect(diags.length).toBe(1);
    expect(diags[0].message).toContain('TS2322');
    lsp.shutdown();
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/lsp/LspClient.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/lsp/LspClient.ts`:
```ts
import { spawn, type ChildProcess } from 'node:child_process';

export interface LspDiagnostic { line: number; character: number; endLine: number; endCharacter: number; severity: number; message: string }
export interface SpawnFn { (cmd: string, args: string[], opts: unknown): ChildProcess }
export interface LspClientDeps { spawnImpl?: SpawnFn }

export class LspClient {
  private nextId = 1;
  private pending = new Map<number, { resolve: (v: unknown) => void; reject: (e: Error) => void }>();
  private diagnostics = new Map<string, LspDiagnostic[]>();
  private child: ChildProcess;
  private buf = '';

  constructor(private rootDir: string, private command: string, private args: string[], private deps: LspClientDeps = {}) {
    this.child = (deps.spawnImpl ?? spawn)(command, args, { stdio: ['pipe', 'pipe', 'pipe'] });
    (this.child.stdout as NodeJS.ReadableStream).on('data', (d: Buffer) => { this.buf += d.toString('utf8'); this.drain(); });
  }

  private drain(): void {
    for (;;) {
      const m = /^Content-Length: (\d+)\r?\n\r?\n/.exec(this.buf);
      if (!m) { this.buf = this.buf.slice(-1000); return; }
      const len = Number(m[1]);
      if (this.buf.length < m[0].length + len) return;
      const raw = this.buf.slice(m[0].length, m[0].length + len);
      this.buf = this.buf.slice(m[0].length + len);
      try { this.handle(JSON.parse(raw)); } catch { /* skip malformed frame */ }
    }
  }

  private handle(msg: Record<string, unknown>): void {
    if (msg.method === 'textDocument/publishDiagnostics') {
      const p = msg.params as { uri: string; diagnostics: Array<{ range: { start: { line: number; character: number }; end: { line: number; character: number } }; severity?: number; message: string }> };
      this.diagnostics.set(p.uri, p.diagnostics.map(d => ({
        line: d.range.start.line, character: d.range.start.character,
        endLine: d.range.end.line, endCharacter: d.range.end.character,
        severity: d.severity ?? 1, message: d.message
      })));
      return;
    }
    const id = msg.id as number;
    const pending = this.pending.get(id);
    if (!pending) return;
    this.pending.delete(id);
    if (msg.error) pending.reject(new Error(JSON.stringify(msg.error)));
    else pending.resolve(msg.result);
  }

  private request(method: string, params: unknown): Promise<unknown> {
    const id = this.nextId++;
    return new Promise((resolve, reject) => {
      this.pending.set(id, { resolve, reject });
      const body = JSON.stringify({ jsonrpc: '2.0', id, method, params });
      this.child.stdin.write(`Content-Length: ${Buffer.byteLength(body)}\r\n\r\n${body}`);
    });
  }

  private notify(method: string, params: unknown): void {
    const body = JSON.stringify({ jsonrpc: '2.0', method, params });
    this.child.stdin.write(`Content-Length: ${Buffer.byteLength(body)}\r\n\r\n${body}`);
  }

  async initialize(): Promise<void> {
    await this.request('initialize', { processId: process.pid, rootUri: `file://${this.rootDir}`, capabilities: {}, workspaceFolders: [{ uri: `file://${this.rootDir}`, name: 'ws' }] });
    this.notify('initialized', {});
  }

  didOpen(uri: string, text: string): void {
    this.notify('textDocument/didOpen', { textDocument: { uri, languageId: 'typescript', version: 1, text } });
  }

  didChange(uri: string, text: string, version: number): void {
    this.notify('textDocument/didChange', { textDocument: { uri, version }, contentChanges: [{ text }] });
  }

  pullDiagnostics(uri: string): LspDiagnostic[] {
    return this.diagnostics.get(uri) ?? [];
  }

  shutdown(): void {
    try { this.child.stdin.end(); } catch { /* ignore */ }
    this.child.kill();
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/lsp/LspClient.spec.ts`
Expected: PASS。

- [ ] **Step 5: 编写 run_tests 工具与修复循环失败测试**

`packages/core/src/coding/fixloop.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { runTestFixLoop } from './fixloop';

describe('test fix loop', () => {
  it('retries with diagnostics feedback until tests pass', async () => {
    let failCount = 2;
    const feedbacks: string[] = [];
    const result = await runTestFixLoop({
      runTests: async () => ({ pass: failCount-- <= 0, output: '1 failed' }),
      pullDiagnostics: async () => ['TS2322: type error at a.ts:1'],
      runEngine: async (fb) => { feedbacks.push(fb); return 'fixed'; }
    }, { maxRetries: 3 });
    expect(result.passed).toBe(true);
    expect(feedbacks.length).toBe(2);
    expect(feedbacks[0]).toContain('TS2322');
  });

  it('gives up after maxRetries exhausted', async () => {
    const result = await runTestFixLoop({
      runTests: async () => ({ pass: false, output: 'still failing' }),
      pullDiagnostics: async () => [],
      runEngine: async () => 'x'
    }, { maxRetries: 1 });
    expect(result.passed).toBe(false);
  });
});
```

- [ ] **Step 6: 编写实现**

`packages/core/src/coding/fixloop.ts`:
```ts
export interface TestRunResult { pass: boolean; output: string }
export interface FixLoopDeps {
  runTests(): Promise<TestRunResult>;
  pullDiagnostics(): Promise<string[]>;
  runEngine(feedback: string): Promise<string>;
}
export interface FixLoopResult { passed: boolean; attempts: number }
export interface FixLoopOpts { maxRetries?: number; onAttempt?: (attempt: number) => void }

export async function runTestFixLoop(deps: FixLoopDeps, opts: FixLoopOpts = {}): Promise<FixLoopResult> {
  const max = opts.maxRetries ?? 3;
  for (let attempt = 0; attempt <= max; attempt++) {
    opts.onAttempt?.(attempt);
    const t = await deps.runTests();
    if (t.pass) return { passed: true, attempts: attempt };
    if (attempt === max) return { passed: false, attempts: attempt + 1 };
    const diags = await deps.pullDiagnostics();
    const feedback = `Test failed (attempt ${attempt + 1}):\n${t.output}${diags.length ? '\nDiagnostics:\n' + diags.join('\n') : ''}`;
    await deps.runEngine(feedback);
  }
  return { passed: false, attempts: max + 1 };
}
```

`packages/core/src/coding/lsp/runTests.ts`(注册 `run_tests` 工具):
```ts
import type { ToolRegistry } from '../../agent/ToolRegistry';
import type { Sandbox } from '../../sandbox/Sandbox';
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';

const exec = promisify(execFile);

export interface RunTestsDeps { execImpl?: (cmd: string, opts: { cwd: string; env: Record<string, string> }) => Promise<{ stdout: string; stderr: string }> }

export function registerRunTestsTool(registry: ToolRegistry, sandbox: Sandbox, deps: RunTestsDeps = {}): void {
  const run = deps.execImpl ?? (async (cmd, opts) => {
    try { return await exec(cmd, { cwd: opts.cwd, env: opts.env, shell: '/bin/sh', timeout: 60_000 }); }
    catch (e) { const err = e as { stdout?: string; stderr?: string }; return { stdout: err.stdout ?? '', stderr: err.stderr ?? String(e) }; }
  });
  registry.register({
    name: 'run_tests', description: 'Run the project test suite and report pass/fail output',
    parameters: { type: 'object', properties: { command: { type: 'string', description: 'override test command' } } }
  }, async (args, ctx) => {
    const command = String(args.command ?? 'npm test');
    sandbox.assertCommand(command);
    const { stdout, stderr } = await run(command, { cwd: ctx.cwd, env: ctx.env });
    const output = `${stdout}${stderr ? '\n[stderr]\n' + stderr : ''}`.trim();
    return { ok: !stderr, output };
  });
}
```

- [ ] **Step 7: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/fixloop.spec.ts src/coding/lsp`
Expected: PASS。

- [ ] **Step 8: Commit**

```bash
git add packages/core/src/coding/lsp packages/core/src/coding/fixloop.ts packages/core/src/coding/fixloop.spec.ts packages/core/src/coding/index.ts
git commit -m "feat(core): headless LSP client and test-fix loop (E7/E8)"
```

---

### Task 6: 代码索引基础设施(E1/L27)+ jarvisignore 过滤(L28)+ search_code

**Files:**
- Create: `packages/core/src/coding/index/chunker.ts`
- Create: `packages/core/src/coding/index/IndexStore.ts`
- Create: `packages/core/src/coding/index/index.spec.ts`
- Modify: `apps/desktop/src/main/ipc/coding.ts`(index.* IPC + better-sqlite3 adapter)

**Interfaces:**
- Consumes: M3 `parseIgnorePatterns`/`isIgnored`(L28)。
- Produces:
  - `Chunk { id; path; startLine; endLine; text }`;`chunkFile(path, text, maxLines?): Chunk[]` — 行块切分(空行/函数类签名边界 + 最长限制)。
  - `EmbeddingFn = (text: string) => Promise<number[]>`
  - `IndexRow { chunkId; path; startLine; endLine; text; embedding }`
  - `IndexStoreAdapter { upsert(rows): void; all(): IndexRow[]; remove(path): void }`
  - `IndexStore { indexFiles(files): Promise<void>; search(query, k?): Promise<IndexRow[]> }` — cosine 相似度检索。
  - `hashEmbedding(text, dim?): number[]` — 确定性哈希嵌入(本地/MVP;生产注入 Provider embedding)。
  - `registerSearchCodeTool(registry, index)` — `search_code` 工具。
  - main:`createCodeIndexAdapter(db)` 读写 `code_chunks` 表;IPC `index.reindex { workspaceRoot }`、`index.search { query, limit }`。

- [ ] **Step 1: 编写失败测试**

`packages/core/src/coding/index/index.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { chunkFile } from './chunker';
import { IndexStore, hashEmbedding, type IndexRow, type IndexStoreAdapter } from './IndexStore';
import { parseIgnorePatterns, isIgnored } from '../../sandbox/ignore';

describe('chunker', () => {
  it('splits a file into line-block chunks', () => {
    const text = 'export function a() {}\n\n\n\nexport function b() {}\n';
    const chunks = chunkFile('src/a.ts', text, 2);
    expect(chunks.length).toBeGreaterThanOrEqual(2);
    expect(chunks[0].startLine).toBe(1);
  });
});

describe('IndexStore', () => {
  const mkAdapter = (): IndexStoreAdapter => {
    let rows: IndexRow[] = [];
    return {
      upsert(r) { rows = r; },
      all() { return rows; },
      remove(path) { rows = rows.filter(r => r.path !== path); }
    };
  };

  it('indexes files and finds relevant chunk by semantic query', async () => {
    const store = new IndexStore(mkAdapter(), hashEmbedding);
    await store.indexFiles([
      { path: 'src/add.ts', text: 'export function add(a: number, b: number) { return a + b; }' },
      { path: 'src/del.ts', text: 'export function removeAll() { /* nope */ }' }
    ]);
    const res = await store.search('export function add a b', 1);
    expect(res[0].path).toBe('src/add.ts');
  });

  it('filters ignored paths via jarvisignore', () => {
    const rx = parseIgnorePatterns(['node_modules/']);
    expect(isIgnored('/ws/node_modules/x.js', rx)).toBe(true);
    expect(isIgnored('/ws/src/a.ts', rx)).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/index/index.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/index/chunker.ts`:
```ts
export interface Chunk { id: string; path: string; startLine: number; endLine: number; text: string }

export function chunkFile(path: string, text: string, maxLines = 120): Chunk[] {
  const lines = text.split('\n');
  const out: Chunk[] = [];
  let start = 0;
  const sigRe = /^(export\s+)?(default\s+)?(async\s+)?(function|class|interface|type|const\s+\w+\s*=)\b/;
  for (let i = 0; i <= lines.length; i++) {
    const atEnd = i === lines.length;
    const tooLong = i - start >= maxLines;
    const blankBoundary = !atEnd && /^\s*$/.test(lines[i]) && i - start >= 4;
    const sigBoundary = !atEnd && sigRe.test(lines[i]) && i - start >= 4;
    if (i > start && (atEnd || tooLong || blankBoundary || sigBoundary)) {
      out.push({ id: `${path}:${start + 1}-${i}`, path, startLine: start + 1, endLine: i, text: lines.slice(start, i).join('\n') });
      start = i;
    }
  }
  return out;
}
```

`packages/core/src/coding/index/IndexStore.ts`:
```ts
import { chunkFile, type Chunk } from './chunker';

export type EmbeddingFn = (text: string) => Promise<number[]>;
export interface IndexRow { chunkId: string; path: string; startLine: number; endLine: number; text: string; embedding: number[] }
export interface IndexStoreAdapter { upsert(rows: IndexRow[]): void; all(): IndexRow[]; remove(path: string): void }
export interface IndexableFile { path: string; text: string }

export function cosine(a: number[], b: number[]): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) { dot += a[i] * b[i]; na += a[i] * a[i]; nb += b[i] * b[i]; }
  return na === 0 || nb === 0 ? 0 : dot / (Math.sqrt(na) * Math.sqrt(nb));
}

export class IndexStore {
  constructor(private adapter: IndexStoreAdapter, private embed: EmbeddingFn) {}

  async indexFiles(files: IndexableFile[]): Promise<void> {
    const rows: IndexRow[] = [];
    for (const f of files) {
      this.adapter.remove(f.path);
      for (const c of chunkFile(f.path, f.text)) {
        rows.push({ chunkId: c.id, path: c.path, startLine: c.startLine, endLine: c.endLine, text: c.text, embedding: await this.embed(c.text) });
      }
    }
    this.adapter.upsert(rows);
  }

  async search(query: string, k = 5): Promise<IndexRow[]> {
    const qv = await this.embed(query);
    return this.adapter.all()
      .map(r => ({ r, s: cosine(qv, r.embedding) }))
      .sort((a, b) => b.s - a.s)
      .slice(0, k)
      .map(x => x.r);
  }
}

export function hashEmbedding(text: string, dim = 32): number[] {
  const v = new Array(dim).fill(0);
  for (const tok of text.split(/\s+/)) {
    if (!tok) continue;
    let h = 0;
    for (const ch of tok) h = (h * 31 + ch.charCodeAt(0)) >>> 0;
    v[h % dim] += 1;
  }
  return v;
}

export type { Chunk };
```

`packages/core/src/coding/index/searchTool.ts`:
```ts
import type { ToolRegistry } from '../../agent/ToolRegistry';
import type { IndexStore } from './IndexStore';

export function registerSearchCodeTool(registry: ToolRegistry, index: IndexStore): void {
  registry.register({
    name: 'search_code', description: 'Semantic search over the indexed workspace code',
    parameters: { type: 'object', properties: { query: { type: 'string' }, limit: { type: 'number' } }, required: ['query'] }
  }, async (args) => {
    const rows = await index.search(String(args.query), Number(args.limit ?? 5));
    return { ok: true, output: rows.map(r => `${r.path}:${r.startLine}-${r.endLine}\n${r.text}`).join('\n---\n') };
  });
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/index/index.spec.ts`
Expected: PASS。

- [ ] **Step 5: main 接线 — 索引 IPC**

`apps/desktop/src/main/ipc/coding.ts` 追加:
```ts
import { IndexStore, hashEmbedding, type IndexRow } from '@jarvis/core';

export function createCodeIndexAdapter(db: Database.Database) {
  const del = db.prepare('DELETE FROM code_chunks WHERE path = ?');
  const ins = db.prepare('INSERT INTO code_chunks (id, path, start_line, end_line, text, embedding_json, updated_at) VALUES (?,?,?,?,?,?,?)');
  return {
    upsert(rows: IndexRow[]) { for (const r of rows) ins.run(r.chunkId, r.path, r.startLine, r.endLine, r.text, JSON.stringify(r.embedding), new Date().toISOString()); },
    all(): IndexRow[] { return (db.prepare('SELECT * FROM code_chunks').all() as Array<Record<string, unknown>>).map(r => ({ chunkId: r.id as string, path: r.path as string, startLine: r.start_line as number, endLine: r.end_line as number, text: r.text as string, embedding: JSON.parse(r.embedding_json as string) as number[] })); },
    remove(path: string) { del.run(path); }
  };
}

// index.reindex: 遍历工作区(跳过 jarvisignore/node_modules),读取文件 → index.indexFiles
// index.search: 返回 IndexStore.search(query, limit)
```

IpcRouter 注册 `index.reindex`/`index.search`;`IndexStore` 的 `embeddingFn` 在 main 构造:优先调用户 Provider embedding API(M1 ModelRouter 扩展),不可用时回退 `hashEmbedding`。

- [ ] **Step 6: 增量更新(fs.watch + debounce)**

main 中注册 `fs.watch(workspaceRoot, { recursive: true })` 回调,`debounce(800ms)` 后对变更文件重新 `indexFiles`(读取新内容);删除文件调 `adapter.remove(path)`。debounce 为 20 行纯函数:
```ts
export function debounce<T extends unknown[]>(fn: (...args: T) => void, ms: number) {
  let t: ReturnType<typeof setTimeout> | undefined;
  return (...args: T) => { if (t) clearTimeout(t); t = setTimeout(() => fn(...args), ms); };
}
```

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/coding/index apps/desktop/src/main/ipc/coding.ts apps/desktop/src/main/ipc/IpcRouter.ts apps/desktop/src/main/daemon
git commit -m "feat(core): code index with embeddings, search_code tool, jarvisignore filter (E1/L27/L28)"
```

---

### Task 7: 轻量代码面板(E11/K3)— 文件树 + 只读预览 + 分屏(K7)

**Files:**
- Create: `packages/core/src/coding/filetree.ts`
- Create: `packages/core/src/coding/filetree.spec.ts`
- Create: `apps/desktop/src/main/ipc/workspace.ts`
- Create: `apps/desktop/src/renderer/src/components/coding/FileTree.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/FileTree.spec.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/CodePreview.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/SplitLayout.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/SplitLayout.spec.tsx`
- Create: `apps/desktop/src/renderer/src/pages/CodingPanelPage.tsx`
- Create: `apps/desktop/src/renderer/src/CodingPanelPage.spec.tsx`

**Interfaces:**
- Consumes: M3 `isIgnored`;M0 i18n。
- Produces:
  - `TreeNode { name; path; type:'file'|'dir'; children }`;`TreeFs { listDir(p): Array<{ name; isDir }> }`
  - `buildTree(root, fs, isIgnored, maxDepth?): TreeNode[]` — 递归构建,跳过 `.jarvis` 与 ignored,目录优先排序。
  - main `workspace.tree { workspaceRoot }`/`workspace.read { path }` IPC(读文件前 Sandbox.assertRead)。
  - `FileTree`(点击文件 → onSelect(path))、`CodePreview`(只读高亮,复用 M1 高亮栈)、`SplitLayout`(左右分栏可拖拽调整宽度,K7)、`CodingPanelPage`(左侧文件树 + 右侧 Tab:预览/Diff)。

- [ ] **Step 1: 编写文件树失败测试**

`packages/core/src/coding/filetree.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildTree, type TreeFs } from './filetree';

const fs: TreeFs = {
  listDir(p) {
    if (p === '/ws') return [{ name: 'src', isDir: true }, { name: 'README.md', isDir: false }, { name: 'node_modules', isDir: true }, { name: '.jarvis', isDir: true }];
    if (p === '/ws/src') return [{ name: 'a.ts', isDir: false }];
    return [];
  }
};

describe('buildTree', () => {
  it('builds nested tree, filters ignored and dot-jarvis, sorts dirs first', () => {
    const tree = buildTree('/ws', fs, (rel) => rel.startsWith('node_modules') || rel.includes('.jarvis'));
    expect(tree.map(n => n.name)).toEqual(['src', 'README.md']);
    expect(tree[0].children[0].name).toBe('a.ts');
    expect(tree.find(n => n.name === 'README.md')!.type).toBe('file');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/filetree.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/filetree.ts`:
```ts
export interface TreeNode { name: string; path: string; type: 'file' | 'dir'; children: TreeNode[] }
export interface TreeEntry { name: string; isDir: boolean }
export interface TreeFs { listDir(p: string): TreeEntry[] }

export function buildTree(root: string, fs: TreeFs, isIgnored: (rel: string) => boolean, maxDepth = 12): TreeNode[] {
  const walk = (dir: string, relPrefix: string, depth: number): TreeNode[] => {
    if (depth > maxDepth) return [];
    const out: TreeNode[] = [];
    for (const e of fs.listDir(dir)) {
      if (e.name === '.jarvis') continue;
      if (e.name.startsWith('.') && e.name !== '.git') continue;
      const rel = relPrefix ? `${relPrefix}/${e.name}` : e.name;
      if (isIgnored(rel)) continue;
      out.push(e.isDir
        ? { name: e.name, path: rel, type: 'dir', children: walk(`${dir}/${e.name}`, rel, depth + 1) }
        : { name: e.name, path: rel, type: 'file', children: [] });
    }
    out.sort((a, b) => a.type === b.type ? a.name.localeCompare(b.name) : a.type === 'dir' ? -1 : 1);
    return out;
  };
  return walk(root, '', 0);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/filetree.spec.ts`
Expected: PASS。

- [ ] **Step 5: main workspace IPC**

`apps/desktop/src/main/ipc/workspace.ts`:
```ts
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join } from 'node:path';
import { buildTree, isIgnored, parseIgnorePatterns } from '@jarvis/core';
import { Sandbox } from '@jarvis/core';

const IGNORE_DEFAULT = ['node_modules/', '.git/', 'dist/', 'build/'];

export function createWorkspaceIpc(getWorkspace: () => string | null) {
  const tree = () => {
    const ws = getWorkspace();
    if (!ws) return [];
    const rx = parseIgnorePatterns(IGNORE_DEFAULT);
    const ignored = (rel: string) => isIgnored('/' + rel, rx);
    return buildTree(ws, { listDir: (p) => readdirSync(p, { withFileTypes: true }).map(e => ({ name: e.name, isDir: e.isDirectory() })) }, ignored);
  };
  const read = (rel: string) => {
    const ws = getWorkspace();
    if (!ws) return { ok: false as const, error: 'no workspace' };
    const sb = new Sandbox(ws, { level: 'readwrite', allowDomains: [], allowCommands: [] });
    const abs = join(ws, rel);
    try { sb.assertRead(abs); } catch (e) { return { ok: false as const, error: (e as Error).message }; }
    return { ok: true as const, content: readFileSync(abs, 'utf8') };
  };
  return { tree, read };
}
```

- [ ] **Step 6: 编写组件与冒烟测试**

`apps/desktop/src/renderer/src/components/coding/FileTree.tsx`:
```tsx
import type { TreeNode } from '@jarvis/core';

export function FileTree({ nodes, onSelect, depth = 0 }: { nodes: TreeNode[]; onSelect: (path: string) => void; depth?: number }) {
  return (
    <ul className="file-tree" data-testid="file-tree">
      {nodes.map(n => (
        <li key={n.path} style={{ paddingLeft: depth * 12 }}>
          {n.type === 'dir'
            ? <span data-testid="tree-dir">{n.name}/</span>
            : <button data-testid="tree-file" onClick={() => onSelect(n.path)}>{n.name}</button>}
          {n.children.length > 0 && <FileTree nodes={n.children} onSelect={onSelect} depth={depth + 1} />}
        </li>
      ))}
    </ul>
  );
}
```

`apps/desktop/src/renderer/src/components/coding/CodePreview.tsx`:
```tsx
export function CodePreview({ path, code }: { path: string; code: string }) {
  return (
    <div data-testid="code-preview" className="code-preview">
      <div className="code-preview__path">{path}</div>
      <pre><code>{code}</code></pre>
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/coding/SplitLayout.tsx`:
```tsx
import { useRef, useState } from 'react';

export function SplitLayout({ left, right, minLeft = 220, defaultRatio = 0.35 }: { left: React.ReactNode; right: React.ReactNode; minLeft?: number; defaultRatio?: number }) {
  const ref = useRef<HTMLDivElement>(null);
  const [ratio, setRatio] = useState(defaultRatio);
  const dragging = useRef(false);

  const onMove = (e: MouseEvent) => {
    if (!dragging.current || !ref.current) return;
    const r = ref.current.getBoundingClientRect();
    setRatio(Math.min(0.7, Math.max(minLeft / r.width, (e.clientX - r.left) / r.width)));
  };
  const onUp = () => { dragging.current = false; document.removeEventListener('mousemove', onMove); document.removeEventListener('mouseup', onUp); };

  return (
    <div ref={ref} data-testid="split-layout" className="split-layout">
      <div style={{ width: `${ratio * 100}%` }} className="split-layout__left">{left}</div>
      <div
        className="split-layout__divider"
        onMouseDown={(e) => { dragging.current = true; document.addEventListener('mousemove', onMove); document.addEventListener('mouseup', onUp); e.preventDefault(); }}
      />
      <div className="split-layout__right">{right}</div>
    </div>
  );
}
```

`apps/desktop/src/renderer/src/pages/CodingPanelPage.tsx`:
```tsx
import { useEffect, useState } from 'react';
import { useTranslation } from 'react-i18next';
import { FileTree } from '../components/coding/FileTree';
import { CodePreview } from '../components/coding/CodePreview';
import { SplitLayout } from '../components/coding/SplitLayout';
import type { TreeNode } from '@jarvis/core';

export function CodingPanelPage() {
  const { t } = useTranslation('common');
  const [tree, setTree] = useState<TreeNode[]>([]);
  const [preview, setPreview] = useState<{ path: string; code: string } | null>(null);
  const refresh = async () => setTree((await window.jarvis.invoke('workspace.tree')) as TreeNode[]);
  useEffect(() => { void refresh(); }, []);
  const open = async (path: string) => {
    const r = (await window.jarvis.invoke('workspace.read', path)) as { ok: boolean; content?: string; error?: string };
    if (r.ok && r.content != null) setPreview({ path, code: r.content });
  };
  return (
    <div data-testid="coding-panel">
      <h2>{t('menu.coding')}</h2>
      <SplitLayout
        left={<FileTree nodes={tree} onSelect={(p) => void open(p)} />}
        right={preview ? <CodePreview path={preview.path} code={preview.code} /> : <div>{t('coding.selectFile')}</div>}
      />
    </div>
  );
}
```

`apps/desktop/src/renderer/src/pages/CodingPanelPage.spec.tsx`:
```tsx
import { describe, it, expect, beforeAll } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import { getResources } from '@jarvis/i18n';
import { CodingPanelPage } from './CodingPanelPage';

beforeAll(async () => {
  await i18n.use(initReactI18next).init({ resources: getResources(), lng: 'zh-CN', ns: ['common'], defaultNS: 'common' });
  (window as unknown as { jarvis: unknown }).jarvis = {
    invoke: async (m: string) => m === 'workspace.tree' ? [{ name: 'src', path: 'src', type: 'dir', children: [{ name: 'a.ts', path: 'src/a.ts', type: 'file', children: [] }] }] : { ok: false, error: 'x' },
    onDidReceive: () => () => {}
  };
});

describe('CodingPanelPage', () => {
  it('renders file tree', async () => {
    render(<CodingPanelPage />);
    await waitFor(() => expect(screen.getByTestId('file-tree')).toBeTruthy());
    expect(screen.getByTestId('tree-file').textContent).toBe('a.ts');
  });
});
```

- [ ] **Step 7: 运行测试确认通过 + i18n 对称**

zh-CN:`"menu.coding": "代码面板"`,`"coding.selectFile": "从左侧选择文件预览"`;en 对应。

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/pages/CodingPanelPage.spec.tsx src/renderer/src/components/coding && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 8: Commit**

```bash
git add packages/core/src/coding/filetree.ts packages/core/src/coding/filetree.spec.ts apps/desktop/src/main/ipc/workspace.ts apps/desktop/src/renderer/src/components/coding apps/desktop/src/renderer/src/pages/CodingPanelPage.tsx packages/i18n/locales
git commit -m "feat(coding-ui): file tree, read-only preview, split layout panel (E11/K3/K7)"
```

---

### Task 8: Diff 面板(E9 UI)+ @ 引用 Picker(E6 UI)+ Plan 徽标(E10)+ 会话恢复(E15)

**Files:**
- Create: `packages/core/src/coding/session.ts`
- Create: `packages/core/src/coding/session.spec.ts`
- Create: `apps/desktop/src/renderer/src/components/coding/DiffPanel.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/DiffPanel.spec.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/MentionPicker.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/MentionPicker.spec.tsx`
- Create: `apps/desktop/src/renderer/src/components/coding/PlanModeBadge.tsx`
- Modify: `apps/desktop/src/main/ipc/coding.ts`(diff.applyAll)

**Interfaces:**
- Consumes: Task 1 diff、Task 3 mention、Task 4 plan;M0 schema(chat_messages);M1 ContextManager。
- Produces:
  - main `diff.applyAll { taskId, path, accepts: boolean[] }` — 读 base(git HEAD 或快照)与 modified(当前工作区),`groupHunks` + `applyHunks` 后写回文件。
  - `DiffPanel`(逐 hunk Accept/Reject → 全部决定后调 `diff.applyAll` 并刷新文件树)。
  - `MentionPicker`(输入框 `@` 触发,debounce 调 `mention.search`,选择后 onSelect(candidate));main `mention.search { workspaceRoot, query }`(文件树 + search_code + agent 列表模糊)。
  - `PlanModeBadge`(显示 Task 当前 plan_only 状态)。
  - `SessionMessage { role; content }`;`SessionStoreAdapter { getMessages(taskId); getSummary(taskId); saveSummary(taskId, text) }`
  - `resumeSession(adapter, taskId, budgetTokens, summarize): Promise<SessionMessage[]>` — 有摘要用摘要,无摘要且超出预算则 summarize 被丢弃的前段并缓存;返回最近消息尾 + 摘要前缀。
  - main:`task.resume { taskId }` — 重建 engine 上下文后继续执行(E15)。

- [ ] **Step 1: 编写会话恢复失败测试**

`packages/core/src/coding/session.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { resumeSession, estimateTokens, type SessionStoreAdapter, type SessionMessage } from './session';

const mkMsgs = (n: number): SessionMessage[] => Array.from({ length: n }, (_, i) => ({ role: 'user' as const, content: `msg ${i} `.repeat(60) }));

describe('resumeSession', () => {
  it('reuses a cached summary without summarizing again', async () => {
    let summarizeCalls = 0;
    const adapter: SessionStoreAdapter = {
      getMessages: async () => mkMsgs(50),
      getSummary: async () => 'cached summary',
      saveSummary: async () => { summarizeCalls++; }
    };
    const out = await resumeSession(adapter, 't1', 2000, async () => 'fresh summary');
    expect(out[0].content).toContain('cached summary');
    expect(summarizeCalls).toBe(0);
  });

  it('summarizes dropped head when over budget and no summary exists', async () => {
    const saved: string[] = [];
    const adapter: SessionStoreAdapter = {
      getMessages: async () => mkMsgs(50),
      getSummary: async () => null,
      saveSummary: async (_t, s) => { saved.push(s); }
    };
    const out = await resumeSession(adapter, 't2', 500, async () => 'fresh summary');
    expect(out.some(m => m.content.includes('fresh summary'))).toBe(true);
    expect(saved).toEqual(['fresh summary']);
  });

  it('keeps all messages when within budget', async () => {
    const adapter: SessionStoreAdapter = {
      getMessages: async () => [{ role: 'user', content: 'hi' }],
      getSummary: async () => null,
      saveSummary: async () => {}
    };
    const out = await resumeSession(adapter, 't3', 100_000, async () => 'x');
    expect(out.length).toBe(1);
  });
});

describe('estimateTokens', () => {
  it('approximates tokens from length', () => {
    expect(estimateTokens('abcde')).toBe(3);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/session.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/session.ts`:
```ts
export interface SessionMessage { role: 'system' | 'user' | 'assistant'; content: string }
export interface SessionStoreAdapter {
  getMessages(taskId: string): Promise<SessionMessage[]>;
  getSummary(taskId: string): Promise<string | null>;
  saveSummary(taskId: string, text: string): Promise<void>;
}

export function estimateTokens(text: string): number {
  return Math.ceil(text.length / 2);
}

export async function resumeSession(
  adapter: SessionStoreAdapter,
  taskId: string,
  budgetTokens: number,
  summarize: (msgs: SessionMessage[]) => Promise<string>,
): Promise<SessionMessage[]> {
  const msgs = await adapter.getMessages(taskId);
  const cached = await adapter.getSummary(taskId);
  let total = cached ? estimateTokens(cached) + 64 : 0;
  const tail: SessionMessage[] = [];
  for (let i = msgs.length - 1; i >= 0; i--) {
    const t = estimateTokens(msgs[i].content);
    if (total + t > budgetTokens) break;
    total += t;
    tail.unshift(msgs[i]);
  }
  if (cached) {
    tail.unshift({ role: 'system', content: `[续接上一任务摘要]\n${cached}` });
    return tail;
  }
  if (msgs.length > tail.length) {
    const dropped = msgs.slice(0, msgs.length - tail.length);
    const s = await summarize(dropped);
    await adapter.saveSummary(taskId, s);
    tail.unshift({ role: 'system', content: `[上一任务摘要]\n${s}` });
  }
  return tail;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/session.spec.ts`
Expected: PASS。

- [ ] **Step 5: main diff.applyAll**

`apps/desktop/src/main/ipc/coding.ts` 追加:
```ts
import { diffLines, groupHunks, applyHunks } from '@jarvis/core';

export function applyDiffToFile(wsRoot: string, path: string, accepts: boolean[]): { ok: boolean; error?: string } {
  const abs = join(wsRoot, path);
  const modified = readFileSync(abs, 'utf8').split('\n');
  // base: git HEAD 优先,否则快照副本
  let base = readFileSync(abs, 'utf8').split('\n');
  try {
    const { stdout } = execSync('git', ['show', `HEAD:${path}`], { cwd: wsRoot });
    base = stdout.replace(/\n$/, '').split('\n');
  } catch { /* 非 git 或未跟踪: base = modified 之前由快照持有 */ }
  const hunks = groupHunks(diffLines(base, modified));
  const result = applyHunks(base, hunks, accepts).join('\n');
  writeFileSync(abs, result);
  return { ok: true };
}
```
注册 `diff.applyAll`;渲染层在全部 hunk 决定后调用并刷新 `workspace.tree`。

- [ ] **Step 6: 编写 DiffPanel 与冒烟测试**

`apps/desktop/src/renderer/src/components/coding/DiffPanel.tsx`:
```tsx
import { useMemo, useState } from 'react';
import { diffLines, groupHunks } from '@jarvis/core';

export function DiffPanel({ taskId, path, base, modified }: { taskId: string; path: string; base: string; modified: string }) {
  const hunks = useMemo(() => groupHunks(diffLines(base.split('\n'), modified.split('\n'))), [base, modified]);
  const [decisions, setDecisions] = useState<Array<'accept' | 'reject' | null>>(hunks.map(() => null));
  const allDone = decisions.every(d => d !== null);

  const decide = (i: number, d: 'accept' | 'reject') => {
    const next = [...decisions]; next[i] = d; setDecisions(next);
  };
  const commit = async () => {
    const accepts = decisions.map(d => d === 'accept');
    await window.jarvis.invoke('diff.applyAll', { taskId, path, accepts });
    window.dispatchEvent(new CustomEvent('jarvis:refresh-tree'));
  };

  return (
    <div data-testid="diff-panel">
      <div className="diff-panel__path">{path}</div>
      {hunks.map((h, i) => (
        <div key={i} data-testid={`hunk-${i}`} className="diff-hunk">
          <span>@@ -{h.oldStart},{h.oldLines} +{h.newStart},{h.newLines} @@</span>
          {h.lines.map((l, j) => (
            <div key={j} className={`diff-line diff-line--${l.type}`}>{l.type === 'add' ? '+' : l.type === 'del' ? '-' : ' '} {l.text}</div>
          ))}
          <button data-testid={`hunk-${i}-accept`} onClick={() => decide(i, 'accept')}>Accept</button>
          <button data-testid={`hunk-${i}-reject`} onClick={() => decide(i, 'reject')}>Reject</button>
        </div>
      ))}
      {allDone && <button data-testid="diff-commit" onClick={() => void commit()}>Apply</button>}
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/coding/DiffPanel.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { DiffPanel } from './DiffPanel';

describe('DiffPanel', () => {
  it('shows hunks and commits accepts', async () => {
    const invoke = vi.fn(async () => ({ ok: true }));
    (window as unknown as { jarvis: unknown }).jarvis = { invoke, onDidReceive: () => () => {} };
    render(<DiffPanel taskId="t1" path="a.ts" base={'const x = 1;\n' + 'y'} modified={'const x = 2;\n' + 'y'} />);
    expect(screen.getByTestId('hunk-0')).toBeTruthy();
    fireEvent.click(screen.getByTestId('hunk-0-accept'));
    fireEvent.click(screen.getByTestId('diff-commit'));
    expect(invoke).toHaveBeenCalledWith('diff.applyAll', { taskId: 't1', path: 'a.ts', accepts: [true] });
  });
});
```

- [ ] **Step 7: 编写 MentionPicker 与冒烟测试**

`apps/desktop/src/renderer/src/components/coding/MentionPicker.tsx`:
```tsx
import { useEffect, useState } from 'react';
import type { MentionCandidate } from '@jarvis/core';

export function MentionPicker({ onSelect, onClose }: { onSelect: (c: MentionCandidate) => void; onClose: () => void }) {
  const [q, setQ] = useState('');
  const [cands, setCands] = useState<MentionCandidate[]>([]);
  useEffect(() => {
    if (!q) { setCands([]); return; }
    const t = setTimeout(async () => {
      const r = (await window.jarvis.invoke('mention.search', q)) as MentionCandidate[];
      setCands(r);
    }, 200);
    return () => clearTimeout(t);
  }, [q]);
  return (
    <div data-testid="mention-picker">
      <input data-testid="mention-input" value={q} onChange={e => setQ(e.target.value)} autoFocus placeholder="@file or @symbol" />
      <ul>{cands.map(c => (
        <li key={c.id}><button data-testid="mention-option" onClick={() => { onSelect(c); onClose(); }}>{c.label} <em>{c.kind}</em></button></li>
      ))}</ul>
    </div>
  );
}
```

`apps/desktop/src/renderer/src/components/coding/MentionPicker.spec.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { MentionPicker } from './MentionPicker';

describe('MentionPicker', () => {
  it('searches and selects a candidate', async () => {
    (window as unknown as { jarvis: unknown }).jarvis = {
      invoke: async (m: string, q: string) => m === 'mention.search' ? [{ id: '1', label: 'src/a.ts', kind: 'file', path: 'src/a.ts' }] : [],
      onDidReceive: () => () => {}
    };
    const onSelect = vi.fn();
    render(<MentionPicker onSelect={onSelect} onClose={() => {}} />);
    fireEvent.change(screen.getByTestId('mention-input'), { target: { value: 'a.t' } });
    await waitFor(() => expect(screen.getByTestId('mention-option')).toBeTruthy());
    fireEvent.click(screen.getByTestId('mention-option'));
    expect(onSelect).toHaveBeenCalledWith(expect.objectContaining({ path: 'src/a.ts' }));
  });
});
```

`apps/desktop/src/renderer/src/components/coding/PlanModeBadge.tsx`:
```tsx
export function PlanModeBadge({ active }: { active: boolean }) {
  if (!active) return null;
  return <span data-testid="plan-badge" className="badge badge--plan">Plan</span>;
}
```

- [ ] **Step 8: main mention.search + task.resume 接线**

`apps/desktop/src/main/ipc/mention.ts`:
```ts
import { IndexStore } from '@jarvis/core';

export async function searchMentions(query: string, index: IndexStore, agents: Array<{ id: string; name: string }>, workspaceTree: () => Array<{ path: string; type: string }>): Promise<Array<{ id: string; label: string; kind: string; path?: string }>> {
  const files = workspaceTree().filter(n => n.type === 'file' && n.path.toLowerCase().includes(query.toLowerCase())).slice(0, 20).map(n => ({ id: n.path, label: n.path, kind: 'file' as const, path: n.path }));
  const codes = (await index.search(query, 10)).map(r => ({ id: `${r.path}:${r.startLine}`, label: `${r.path}:${r.startLine}`, kind: 'symbol' as const, path: r.path }));
  const ags = agents.filter(a => a.name.toLowerCase().includes(query.toLowerCase())).map(a => ({ id: a.id, label: a.name, kind: 'agent' as const }));
  return [...files, ...codes, ...ags];
}
```

`task.resume`:读 task 历史消息(chat_messages 按 session/task 过滤),`resumeSession(adapter, taskId, agent.context_budget_tokens, summarize)` → 用返回上下文重建 engine 并继续执行(E15)。

- [ ] **Step 9: 运行测试确认通过 + i18n 对称**

zh-CN:`"diff.accept": "接受"`,`"diff.reject": "拒绝"`,`"diff.apply": "应用改动"`;en 对应。

Run: `cd apps/desktop && pnpm vitest run src/renderer/src/components/coding && cd packages/core && pnpm vitest run src/coding/session.spec.ts && cd /Users/baofengbaofeng/Workspace/github/baofengbaofeng/Jarvis && node scripts/i18n-check.mjs`
Expected: PASS + 对称。

- [ ] **Step 10: Commit**

```bash
git add packages/core/src/coding/session.ts packages/core/src/coding/session.spec.ts apps/desktop/src/main/ipc/coding.ts apps/desktop/src/main/ipc/mention.ts apps/desktop/src/renderer/src/components/coding/DiffPanel.tsx apps/desktop/src/renderer/src/components/coding/MentionPicker.tsx apps/desktop/src/renderer/src/components/coding/PlanModeBadge.tsx packages/i18n/locales
git commit -m "feat(coding): diff panel accept/reject, mention picker, plan badge, session resume (E9/E6/E10/E15)"
```

---

### Task 9: 并行多 Task(E14)+ 外部 IDE 对接(E12)+ M4 验收

**Files:**
- Create: `packages/core/src/coding/parallel.ts`
- Create: `packages/core/src/coding/parallel.spec.ts`
- Create: `apps/desktop/src/main/external/IdeBridge.ts`
- Create: `apps/desktop/src/main/external/IdeBridge.spec.ts`

**Interfaces:**
- Consumes: M2 TaskOrchestrator;M3 Sandbox(独立 workspace 沙箱)。
- Produces:
  - `runParallel(tasks: ParallelTask[], concurrency: number): Promise<void>` — 并发上限队列执行。
  - E14 隔离保证:每 Task 引擎持有独立 `Sandbox(workspace)` 与独立 MCP session(构造处不共享);本任务以并发上限单测 + 集成说明覆盖。
  - `IdeBridge { start(): Promise<number>; close(): Promise<void> }` — HTTP localhost `/open?file=`(经 resolveFile 解析绝对路径)、`/diff?task=`;`openInExternalIde(cli, file, line?)`、`parseFileArg(spec)`。
  - main:app ready 时启动 IdeBridge + `jarvis open --file` CLI 处理(code -g)。

- [ ] **Step 1: 编写并行失败测试**

`packages/core/src/coding/parallel.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { runParallel } from './parallel';

describe('runParallel', () => {
  it('respects the concurrency cap', async () => {
    let active = 0, peak = 0;
    const run = async () => {
      active++; peak = Math.max(peak, active);
      await new Promise(r => setTimeout(r, 5));
      active--;
    };
    const tasks = Array.from({ length: 8 }, (_, i) => ({ id: `t${i}`, workspace: '/ws', run }));
    await runParallel(tasks, 3);
    expect(peak).toBeLessThanOrEqual(3);
  });

  it('runs all tasks to completion', async () => {
    let done = 0;
    const tasks = Array.from({ length: 5 }, () => ({ id: 't', workspace: '/ws', run: async () => { done++; } }));
    await runParallel(tasks, 2);
    expect(done).toBe(5);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd packages/core && pnpm vitest run src/coding/parallel.spec.ts`
Expected: FAIL。

- [ ] **Step 3: 编写实现**

`packages/core/src/coding/parallel.ts`:
```ts
export interface ParallelTask { id: string; workspace: string; run: () => Promise<unknown> }

export async function runParallel(tasks: ParallelTask[], concurrency: number): Promise<void> {
  const queue = [...tasks];
  const worker = async () => {
    while (queue.length > 0) {
      const t = queue.shift()!;
      await t.run();
    }
  };
  const n = Math.max(1, Math.min(concurrency, tasks.length));
  await Promise.all(Array.from({ length: n }, () => worker()));
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd packages/core && pnpm vitest run src/coding/parallel.spec.ts`
Expected: PASS。

- [ ] **Step 5: 集成说明 — E14 隔离**

在 TaskOrchestrator(M2)的 submit 中已为每 Task 独立构建 engine;本任务补充确保:
- 每 Task 的 `Sandbox` 以其 `task.workspaceId` 为根(M3 Task 2 已如此);
- MCP session 每 Task 一个(不在 engine 间共享)——engine 构造时按 `agent.workspaceId` 启动绑定的 MCP server(G6);
- 并行上限仍由 `agents.concurrency_per_agent`(默认 6)约束。
E14 集成测试 `packages/core/src/task/parallel-integration.spec.ts`(可选,CI 启用):
```ts
// 用两个独立 workspace 的任务同时写不同文件,断言互不干扰且均完成。
```

- [ ] **Step 6: 编写 IdeBridge 失败测试**

`apps/desktop/src/main/external/IdeBridge.spec.ts`:
```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { IdeBridge, parseFileArg, openInExternalIde } from './IdeBridge';

describe('parseFileArg', () => {
  it('parses path:line', () => {
    expect(parseFileArg('src/a.ts:12')).toEqual({ file: 'src/a.ts', line: 12 });
    expect(parseFileArg('src/a.ts')).toEqual({ file: 'src/a.ts', line: undefined });
  });
});

describe('openInExternalIde', () => {
  it('invokes cli with -g and line', () => {
    const cli = vi.fn();
    openInExternalIde(cli, 'src/a.ts', 12);
    expect(cli).toHaveBeenCalledWith(['-g', 'src/a.ts:12']);
  });
});

describe('IdeBridge HTTP', () => {
  it('serves /open and /diff', async () => {
    const bridge = new IdeBridge({
      resolveFile: (f) => f === 'src/a.ts' ? `/ws/${f}` : null,
      resolveTaskDiff: (t) => t === 't1' ? { path: 'src/a.ts', diff: '@@ -1 +1 @@\n-const x = 1;\n+const x = 2;\n' } : null
    });
    const port = await bridge.start();
    const base = `http://127.0.0.1:${port}`;
    const openRes = await fetch(`${base}/open?file=src%2Fa.ts`).then(r => r.json());
    expect(openRes).toEqual({ ok: true, path: '/ws/src/a.ts' });
    const diffRes = await fetch(`${base}/diff?task=t1`).then(r => r.json());
    expect(diffRes.path).toBe('src/a.ts');
    await bridge.close();
  });
  afterEach(() => { vi.restoreAllMocks(); });
});
```

- [ ] **Step 7: 运行测试确认失败**

Run: `cd apps/desktop && pnpm vitest run src/main/external/IdeBridge.spec.ts`
Expected: FAIL。

- [ ] **Step 8: 编写实现**

`apps/desktop/src/main/external/IdeBridge.ts`:
```ts
import { createServer, type Server } from 'node:http';

export interface IdeBridgeOptions {
  port?: number;
  resolveFile: (rel: string) => string | null;
  resolveTaskDiff: (taskId: string) => { path: string; diff: string } | null;
}

export class IdeBridge {
  private server?: Server;
  constructor(private opts: IdeBridgeOptions) {}

  async start(): Promise<number> {
    const port = this.opts.port ?? 17891;
    this.server = createServer((req, res) => {
      const url = new URL(req.url ?? '/', `http://127.0.0.1:${port}`);
      if (url.pathname === '/open') {
        const file = url.searchParams.get('file');
        const abs = file ? this.opts.resolveFile(file) : null;
        if (!abs) { res.writeHead(404).end('not found'); return; }
        res.writeHead(200, { 'content-type': 'application/json' }).end(JSON.stringify({ ok: true, path: abs }));
      } else if (url.pathname === '/diff') {
        const taskId = url.searchParams.get('task') ?? '';
        const d = this.opts.resolveTaskDiff(taskId);
        if (!d) { res.writeHead(404).end('not found'); return; }
        res.writeHead(200, { 'content-type': 'application/json' }).end(JSON.stringify(d));
      } else { res.writeHead(404).end(); }
    });
    await new Promise<void>((resolve) => this.server!.listen(port, '127.0.0.1', () => resolve()));
    return port;
  }

  async close(): Promise<void> {
    await new Promise<void>((resolve) => this.server?.close(() => resolve()));
  }
}

export function parseFileArg(spec: string): { file: string; line?: number } {
  const m = /^(.+?):(\d+)$/.exec(spec);
  return m ? { file: m[1], line: Number(m[2]) } : { file: spec };
}

export function openInExternalIde(cli: (args: string[]) => void, file: string, line?: number): void {
  cli(line != null ? ['-g', `${file}:${line}`] : [file]);
}
```

- [ ] **Step 9: main 接线 — 启动桥 + CLI**

app ready 后:
```ts
const bridge = new IdeBridge({
  resolveFile: (rel) => sandboxOk(rel) ? join(workspaceRoot(), rel) : null,
  resolveTaskDiff: (taskId) => loadDiff(taskId) // 复用 diff.applyAll 读取的 base/modified
});
void bridge.start();
```
CLI:`jarvis open --file src/a.ts:12` → 主进程 argv 解析后 `openInExternalIde((args) => spawn('code', args, { stdio: 'inherit' }), file, line)`(仅在装有 VS Code 时生效;否则 fallback 到系统 `open`)。将 `open --file` 子命令处理注册到单实例锁二次启动的 argv 解析中。

- [ ] **Step 10: 运行测试确认通过**

Run: `cd apps/desktop && pnpm vitest run src/main/external/IdeBridge.spec.ts`
Expected: PASS。

- [ ] **Step 11: Commit**

```bash
git add packages/core/src/coding/parallel.ts packages/core/src/coding/parallel.spec.ts apps/desktop/src/main/external/IdeBridge.ts apps/desktop/src/main/external/IdeBridge.spec.ts apps/desktop/src/main/index.ts apps/desktop/src/main/ipc/coding.ts
git commit -m "feat(coding): parallel task runner and external IDE bridge (E14/E12)"
```

---

### M4 验收清单(Self-Review 对照)

**§21 M4 交付(M4 编程 | Diff 面板、@引用、Plan mode、代码索引 | S4 场景):**
- [x] E9 Diff 预览/逐块 Accept/Reject — Task 1(引擎)+ Task 8(DiffPanel)
- [x] E6 @引用 — Task 3(解析/注入)+ Task 8(MentionPicker/mention.search)
- [x] E10 Plan 模式 — Task 4(过滤)+ Task 8(PlanModeBadge)
- [x] E1 代码索引 — Task 6(IndexStore/search_code/增量 fs.watch)
- [x] E15 会话恢复 — Task 8(resumeSession/task.resume)

**§10.6 M4 行(E6–E11,E12,E14,E15,K3,K7,L26–L29):**
- [x] E7 LSP(Agent 侧)— Task 5(LspClient)
- [x] E8 测试运行 + 失败自动修复 — Task 5(run_tests + runTestFixLoop)
- [x] E11/K3 轻量代码面板(文件树+只读预览+Diff)— Task 7
- [x] K7 分屏(聊天+文件/Diff)— Task 7(SplitLayout)
- [x] E12 外部 IDE 对接 — Task 9(IdeBridge + CLI)
- [x] E14 并行多 Task — Task 9(runParallel + 隔离说明)
- [x] L26 Task 级回滚 — Task 2(snapshot/restore/task.rollback)
- [x] L27 代码索引基础设施 — Task 6(IndexStore + embedding)
- [x] L28 jarvisignore — M3 Task 1 已实现,本里程碑复用于索引/文件树
- [x] L29 结构化输出 — Task 4(structuredChat + ajv)

**S4 场景验收(端到端):** 用户在聊天中要求 Agent 修复某函数 → Agent 用 `search_code`/@引用定位 → 编辑(或仅 Plan 只读分析)→ Diff 面板展示改动 → 逐块 Accept/Reject → 应用后 `run_tests` → 失败进入自动修复循环 → 通过;若不满意 `task.rollback` 一键还原。

**M4 已知后置:** 完整 Inline 补全(E5,已排除 Q3:B);Monaco 完整 IDE(K3 降级);sqlc 生成 daemon 侧 tasks 写入(M7);代码索引向量库切 sqlite-vec/LanceDB(可选,embedding 注入已预留);VS Code 插件(可选,后置 V1.0)。
